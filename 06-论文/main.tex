% !TeX program = xelatex
\documentclass[12pt,a4paper]{ctexart}
\usepackage{amsmath,amssymb,bm}
\usepackage{booktabs,array,longtable}
\usepackage{graphicx}
\usepackage[margin=2.5cm]{geometry}
\usepackage{caption}
\captionsetup{font=small,labelsep=quad}
\usepackage[colorlinks=true,linkcolor=black,citecolor=black,urlcolor=black]{hyperref}
\graphicspath{{../05-图表/}}
\numberwithin{equation}{section}
\renewcommand{\arraystretch}{1.25}

% 图未就位时先占位，就位后自动使用（编译前已确认全部就位）；宽度参数适配图的设计宽度
\newcommand{\figplaceholder}[1]{\fbox{\parbox[c][5.5cm][c]{0.86\linewidth}{\centering 图位：#1}}}
\newcommand{\includefig}[3][0.86\linewidth]{\IfFileExists{../05-图表/#2}{\includegraphics[width=#1]{#2}}{\figplaceholder{#3}}}

\title{\textbf{中药材烘干过程的热湿耦合建模、守恒求解与分层验证}\\[2pt]
——基于质量坐标有限体积的四问统一求解}
\author{}
\date{2026 年 9 月 11 日}

\begin{document}
\maketitle

\begin{abstract}
\noindent\textbf{问题与挑战}：中药材（圆柱形，长 25 cm、半径 2 cm，初温 28 ${}^\circ$C、
初始干基含水率 2.55 kg/kg）的烘干涉及预热平衡、恒温干燥、达标判定与收缩变形四个阶段，
本题要求依次给出温度与水分浓度场、达标时间与收缩下的达标时间。挑战在于：
三套附录物性必须按问题分别使用且不得调和；扩散系数 $D$ 对局部含水率与温度强非线性
（尾段变化 16.8 倍）；环境数据仅覆盖 4 h、半径数据在约 21 h 冻结——任何一处口径
处理不当都会使结果偏差数十小时。

\medskip\noindent\textbf{方法}：本文把四问统一为\textbf{一套热湿耦合方程加四个配置开关}：
从干固体守恒、水分守恒与能量守恒第一性原理出发，以干物质标签 $q\in[0,1]$ 为材料坐标
建立质量坐标方程，收缩几何下方程同形、无伪对流项；用守恒型有限体积求解
（表面半单元内阻与外对流阻串联、后向欧拉加局部外推、Picard--Newton 切换、自适应步长），
并以题给参数构建判据族（$Le$、$Bi_m$、$Bi_h$）与比较原理严格下界组织简化与验证。
一条命令复现全部交付文件，连续两次运行哈希一致。

\medskip\noindent\textbf{关键结果}：问题 1 的温度场与解析解逐点对拍最大偏差
$5.9\times10^{-7}\,^{\circ}$C；问题 3 达标时间 $t_f=57.1799$ h（Richardson 外推
57.1636 h，严格下界 15.70 h 通过）；问题 4 达标时间 $t_f=50.8230$ h（收缩下界 19.02 h
通过）。机制归因：附录 4 的扩散系数约为附录 3 的 $1/5.4$（固定半径下烘干时间放大
2.26 倍），半径收缩（$R^2$ 降 64\%）加速 $60.6\%$，净效应使问题 4 反而快 $11.1\%$；
敏感性分析显示边界汇口径（$C_e$）是唯一能量级翻盘的单点（$+22.6$ h）。

\medskip\noindent\textbf{验证与创新}：六层分层验证体系（解析谱解对拍、线性化退化、
严格下界闸门、守恒恒等式、移动域退化、网格收敛）每层明确能排除与不能排除的错误类型，
其中解析解与数值解是两种完全不同数学结构的互证；误差预算由七个可指认误差源确定性
合成为 $\pm1.71$ h；内生收缩分析进一步表明附件 2 的三段收缩可由"理想收缩+玻璃化冻结"
零拟合闭合结构性复现（平台半径偏差 $-2.4\%$），而完整的内生收缩模型
（失水/毛细塌陷/结皮成孔三项第一性原理机制叠加，5 个参数经最小二乘标定并过留出法检验）
以 RMSE 0.047 mm 复现附件 2 全部形态（滞后交叉、平台、三段），耦合内生重解给出
相同的达标时间（50.72 vs 50.82 h，偏差 $-0.22\%$）——收缩被模型内生预测且与实测一致。

\medskip\noindent\textbf{关键词：} 热湿耦合；质量坐标；守恒有限体积；判据族；独立验证；中药材干燥
\end{abstract}

\newpage
\tableofcontents
\newpage
\section{问题重述与审题}

\subsection{问题背景与四问任务}
中药材（圆柱形，长 25 cm、半径 2 cm，初温 28 ${}^\circ$C、初始干基含水率 2.55 kg/kg）
在烘房中烘干，烘房温度与空气水分浓度由附件 1 给出（0--14400 s，60 s 一步）。
四个问题依次为：
\begin{enumerate}
  \item[\textbf{问题 1}] 预热平衡阶段（0--1800 s）的温度与水分浓度场（附录 2 物性），
        按表 1/表 2 格式给出并保存 result1.xlsx；
  \item[\textbf{问题 2}] 整个烘干过程的场（附录 3 物性），按表 3/表 4 格式给出并保存 result2.xlsx；
  \item[\textbf{问题 3}] 各处水分浓度低于 0.15 kg/kg 的达标时间（表 5、result3.xlsx）；
  \item[\textbf{问题 4}] 药材随失水收缩（附件 2 外半径、附录 4 物性）的达标时间（表 6、result4.xlsx）。
\end{enumerate}

\subsection{四问是同一模型的四个配置}
我们不把四问写成四个模型，而是\textbf{一套耦合方程加四个开关}
（物性来源、热湿耦合、几何是否演化、输出形式），见表~\ref{tab:开关}。
这保证了模型的前后自洽：问题 1 是问题 2 的解耦特例，问题 3 是问题 2 加阈值判据，
问题 4 是问题 3 加移动几何与另一套物性。

\begin{table}[htbp]
\centering
\caption{四问的配置开关表（统一模型的四种配置）。}
\label{tab:开关}
\small
\begin{tabular}{lcccc}
\toprule
开关 & 问题 1 & 问题 2 & 问题 3 & 问题 4 \\
\midrule
物性 & 附录 2（常数，$D=f(C)$） & 附录 3 & 附录 3 & 附录 4 \\
热湿耦合 & 解耦（$D$ 不含 $T$） & 双向耦合 & 双向耦合 & 双向耦合 \\
几何 & 固定 $R=0.02$ m & 固定 & 固定 & $R(t)$ 外生（附件 2） \\
环境 & 附件 1（0--1800 s） & 附件 1 + 外推 & 同左 & 同左 \\
输出 & 场（表 1/2，result1） & 场（表 3/4，result2） & 决策量 $t_f$（表 5，result3） & 决策量 $t_f$（表 6，result4） \\
\bottomrule
\end{tabular}
\end{table}

\subsection{主动声明的三个陷阱}
\begin{enumerate}
  \item \textbf{三套物性按问题分别使用，不得调和、平均或用文献值替换。}
        附录 2/3/4 给出的 $\rho$、$c_p$、$k$、$D$ 互不相同（例如附录 4 的 $D$ 在同状态下
        约为附录 3 的 $1/5.4$）。把任一物性常数化后结果"看起来仍合理"（仍落在 2--3 天），
        但违反题目指定的建模方式。本文每个非常数参数都在参数取值表（表~\ref{tab:参数}）
        中写明\emph{在哪个位置、哪个时刻、用哪个公式、什么单位}取值；
        全部符号的量纲逐条核对见附录~\ref{app:量纲}（表~\ref{tab:量纲}）。
  \item \textbf{$D$ 中的 $C$、$T$ 取局部瞬时值，且 $T$ 用开尔文。}
        附录 3/4 的 $D\propto\mathrm{e}^{-3850/T}$：若误用摄氏温度，
        $\mathrm{e}^{-3850/50}\approx0$，程序不报错但全错。
        我们在读入与全程设置开尔文防呆断言（$200<T_K<700$）。
  \item \textbf{附件 1 只有 4 h、附件 2 在约 21 h 后冻结。}
        附件 1 终于 14400 s（4 h），而烘干需数十小时，$t>4$ h 的环境只能外推
        （主口径 = 末值保持，敏感性见 \S\ref{sec:敏感}）；附件 2 的半径在约 21 h 后
        冻结于 1.198 cm 平台，其后只能平台延拓。这两处截断是本题最大的数据局限，
        在 \S\ref{sec:误差} 与 \S\ref{sec:结论} 中显式处理。
\end{enumerate}

\section{模型建立}\label{sec:模型}

\subsection{守恒律与质量坐标}
取干物质为拉格朗日标签。设干基含水率 $U$（kg 水/kg 干物质）、温度 $\varTheta$（K）。
毛细多孔体的热湿传递以扩散—对流方程组描述~\cite{luikov}。对一维径向
（忽略端面与轴向，见闭合假设 4），圆柱内水分守恒与能量守恒为
\begin{equation}
\partial_t U = \frac{1}{r}\partial_r\!\left(rD\,\partial_r U\right),\qquad
a_{\mathrm{eff}}\,\partial_t\varTheta
 = \frac{1}{r}\partial_r\!\left(rk\,\partial_r\varTheta\right),
\label{eq:物理坐标}
\end{equation}
其中 $a_{\mathrm{eff}}=\rho c_p$ 为有效体积热容（显热近似，见闭合假设 3）。
边界为轴心对称与表面 Robin 对流：
\begin{equation}
-D\,\partial_r U\big|_{R}=h_m\,(U_s-C_{\mathrm{env}}(t)),\qquad
-k\,\partial_r\varTheta\big|_{R}=h\,(\varTheta_s-T_a(t)).
\label{eq:边界}
\end{equation}
引入干物质质量坐标 $q\in[0,1]$（$q=0$ 轴心、$q=1$ 表面）。对固定几何
$r(q)=R_0\sqrt{q}$（$r\,dr=\tfrac{R_0^2}{2}dq$，故 $\int U\,dV\propto\int_0^1U\,dq$）；
对问题 4 的收缩几何，声明内部等比收缩 $r(q,t)=R(t)\sqrt{q}$（附件 2 只识别外半径，
内部收缩不可识别，等比是最简闭合）。代入 \eqref{eq:物理坐标} 得\textbf{质量坐标形式}
\begin{equation}
U_t=\partial_q\!\left(\frac{4qD}{R^2}\,U_q\right),\qquad
a_{\mathrm{eff}}\,\varTheta_t
 =\partial_q\!\left(\frac{4qk}{R^2}\,\varTheta_q\right),
\label{eq:质量坐标}
\end{equation}
其中固定几何 $R=R_0$、问题 4 取 $R=R(t)$。需要强调两点（推导见附录~\ref{app:移动域}）：
\textbf{(i)} 在仿射收缩与干固体守恒下，移动域方程与固定域\emph{同形}，
不出现伪对流项；\textbf{(ii)} 移动表面处的 Robin 边界形式不变
（跳条件中的 $\dot R\,U_s$ 项恒等相消），不额外补充任何项。
$q=0$ 处通量系数 $4qD/R^2$ 为零，轴心正则对称在离散时自动满足。

初值 $U(q,0)=2.55$、$\varTheta(q,0)=301.15$ K（$=28\,^{\circ}$C，全部计算用开尔文，
交付的温度表换算回摄氏）。

\subsection{参数取值表（题目参数与声明假设分栏）}
\begin{table}[htbp]
\centering
\caption{参数取值表：每个非常数参数写明公式、单位、取值位置与适用问题。}
\label{tab:参数}
\small
\begin{tabular}{llll}
\toprule
符号 & 数值 / 公式（单位） & 取值位置与方式 & 适用问题 \\
\midrule
\multicolumn{4}{l}{\textit{题目给定（附录与附件）}} \\
$\rho$ & $820$ kg/m$^3$ & 常数 & 问题 1 \\
$\rho(C)$ & $650+128C$；$760+90C$ kg/m$^3$ & 局部瞬时干基 $C$ & 问题 2/3；问题 4 \\
$c_p$ & $2600$ J/(kg$\cdot$K) & 常数 & 问题 1 \\
$c_p(C)$ & $1450+2736C/(C+1)$；$1850+2150C/(C+1)$ & 局部瞬时干基 $C$ & 问题 2/3；问题 4 \\
$k$ & $0.36$ W/(m$\cdot$K) & 常数 & 问题 1 \\
$k(C)$ & $0.21+0.38C/(C+1)$；$0.12+0.20C/(C+1)$ & 局部瞬时干基 $C$ & 问题 2/3；问题 4 \\
$D$ & $7\times10^{-9}\mathrm{e}^{-0.89/C}$ m$^2$/s & 局部 $C$（不依赖 $T$） & 问题 1 \\
$D(C,T)$ & $2.4\times10^{-3}\mathrm{e}^{-0.45/C}\mathrm{e}^{-3850/T}$ & 局部 $C$、局部 $T$（开尔文） & 问题 2/3 \\
$D(C,T)$ & $4.2\times10^{-4}\mathrm{e}^{-0.30/C}\mathrm{e}^{-3850/T}$ & 局部 $C$、局部 $T$（开尔文） & 问题 4 \\
$h$ & $25$ W/(m$^2\cdot$K) & 常数 & 问题 1--4 \\
$h_m$ & $8\times10^{-7}$ m/s & 常数 & 问题 1--4 \\
$T_a(t),C_{\mathrm{env}}(t)$ & 附件 1（0--14400 s，60 s 步） & 分段线性插值 & 问题 1--4 \\
$R(t)$ & 附件 2（0--259200 s，1800 s 步） & PCHIP 保形插值 & 问题 4 \\
几何 & $L_0=0.25$ m，$R_0=0.02$ m & 一维径向（$L_0/R_0=12.5$） & 问题 1--4 \\
初值 & $U_0=2.55$ kg/kg，$\varTheta_0=301.15$ K & 全域均匀 & 问题 1--4 \\
\midrule
\multicolumn{4}{l}{\textit{声明假设（题目未给定，本文声明并前置）}} \\
湿边界闭合 & $C_{\mathrm{env}}$ 直接作 Robin 驱动势 & 有效闭合（\S\ref{sec:闭合}-1） & 问题 1--4 \\
环境外推 & $t>14400$ s 末值保持 & 声明（敏感性见 \S\ref{sec:敏感}） & 问题 2--4 \\
$h,h_m$ 延用 & 附录 2 值延用到问题 2--4 & 声明为参数延用假设 & 问题 2--4 \\
半径延拓 & $t>259200$ s 保持 1.198 cm & 声明 & 问题 4 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{闭合假设（显式声明）}\label{sec:闭合}
\begin{enumerate}
  \item \textbf{湿边界是有效闭合。}附件 1 的空气水分浓度与药材干基含水率基准不同
        （按 kg 水/kg 干空气量级校核：28 ${}^\circ$C 时 0.0196 kg/kg 约对应 81\% RH、
        50 ${}^\circ$C 时 0.0499 约对应 58\% RH，数值合理），本文把 $C_{\mathrm{env}}(t)$
        直接当作与 $h_m$ 配套的驱动势（式~\eqref{eq:边界} 的 Robin 通量）。这是本题最大单点风险：若改用解吸等温线
        $C_e\approx0.137$，$t_f$ 从 57.18 h 变为 79.83 h（$+39.6\%$，见 \S\ref{sec:敏感}）。
        主结果用题目口径，等温线只进敏感性。
  \item \textbf{环境外推}：$t>14400$ s 主口径 = 末值保持（$T_a=50.165\,^{\circ}$C、
        $C_{\mathrm{env}}=0.04986$），分段线性插值保留采样值；对照口径（线性外推、
        末 1 h 均值）进敏感性。
  \item \textbf{不含潜热}：热方程是有效显热近似。量纲论证：$h$ 驱动的显热通量
        $\sim h\Delta T\approx25\times20=500$ W/m$^2$，$h_m$ 驱动的等效潜热功率
        $\sim h_m\Delta C\,\rho L_v$，两者比值 $2.06$--$2.49$——潜热汇与附录物性
        （给定的 $h$ 与实测温升）不能同时自洽，题目口径自洽的读法不含潜热。
        只作论证，不做修正算例。
  \item \textbf{一维径向}：端面效应可忽略（定量）：圆柱 $L=25$ cm、$R=2$ cm，
        端面/侧面积比 $R/L=8\%$；轴向扩散路径（$L/2=12.5$ cm）是径向的 6.25 倍，
        扩散时间尺度约慢 $6.25^2\approx39$ 倍，端面对达标的贡献仅约 $1\%$ 量级，
        一维径向近似合理（$L_0/R_0=12.5$ 仅为几何信息，不保证误差界）。
  \item \textbf{半径插值}：附件 2 用 PCHIP 保形插值（已核查半径为正、单调不增），
        数据点后末值平台延拓；线性插值对照进敏感性（实测 $\Delta t_f=0.002$ h，
        口径不敏感）。
  \item \textbf{$\rho(C)$ 不作体积/质量闭合}：$\rho$、$c_p$、$k$ 逐点取值；
        $\rho$ 只用于有效体积热容 $a_{\mathrm{eff}}=\rho c_p$ 与通量算子
        $\rho_{sv}=\rho/(1+C)$，\textbf{不用于质量/体积闭合}——附录 3/4 的
        $\rho(C)$ 与附件 2 的 $R(t)$ 在干固体质量守恒下不相容
        （附录 3 会给出负含水率 / 要求 $R\ge1.301$ cm，而数据终值为 1.198 cm），
        故 $\rho$ 不进湿分方程。
\end{enumerate}
\section{定量判据}\label{sec:判据}

本节全部由题给参数算出，每条判据配\textbf{反验列}：把简化解的 $t_f$ 与全耦合解
$t_f$（问题 3 设定、N=80 网格、57.2009 h）的实测差值列出。没有反验数字的判据不进表。

\subsection{判据族（Le、Bi\_m、Bi\_h）}
沿路径 $C\in[0.15,2.55]$、$T\in[301.15,323.32]$ K、$R=R_0$ 计算
（$\alpha=k/(\rho c_p)$，逐点取附录公式），结果见表~\ref{tab:判据族}：

\begin{table}[htbp]
\centering
\caption{判据族表：定义、全区间数值、阈值语义、简化决策与反验偏差。}
\label{tab:判据族}
\small
\begin{tabular}{llllll}
\toprule
判据 & 定义 & 附录 3 区间 & 附录 4 区间 & 简化决策 & 反验偏差 \\
\midrule
$Le$ & $\alpha/D$ & $10.69$--$640.6$ & $6.6$--$1209$ & $Le\gg1$：热场准稳态 $T\approx T_a$ & $0.303$ h \\
$Bi_m$ & $h_mR/D$ & $1.18$--$19.9$ & $2.4$--$63.8$ & $Bi_m\to0$：集总（内扩散无阻力） & $-46.03$ h \\
$Bi_h$ & $hR/k$ & $1.04$--$1.86$ & $2.5$--$5.0$ & $Bi_h\to0$：忽略表面热滞后 & $0.075$ h \\
\bottomrule
\end{tabular}
\end{table}

反验读法（每条都是"用了它，$t_f$ 偏多少"）：
\begin{itemize}
  \item \textbf{$Le\gg1$ 支持热场准稳态}：把温度场直接取 $T_a(t)$ 只求湿分，
        实测 $t_f$ 偏差 $0.303$ h（0.5\%）——热瞬态可忽略是\emph{优良}简化；
        本文主模型仍保留双向耦合，此简化只作判据反验。
  \item \textbf{$Bi_m\in[1.2,64]$ 表明内部扩散主导}：集总模型给出 $t_f=11.17$ h，
        比全耦合解快 $46.03$ h（$-80\%$）——\textbf{任何"集总/平均含水率"处理都
        不可接受}，本题必须保留空间分布。这是判据族最直接的定量结论。
  \item \textbf{$Bi_h\sim O(1)$ 表明表面热滞后小}：令 $h\to\infty$（表面温度紧贴环境）
        实测偏差 $0.075$ h（0.13\%）——表面温度滞后可用但不显著，本文未采用该简化。
\end{itemize}

\subsection{达标判据与中心最慢点}
达标时间定义为
\begin{equation}
t_f=\inf\{t:\ \max_{q\in[0,1]}U(q,t)<0.15\ \text{kg/kg}\},
\label{eq:达标}
\end{equation}
用\textbf{未舍入值}判定；$\max$ 在全域重建场上取（不只用单元平均）。
中心是否最慢点：边值 $C_{\mathrm{env}}$ 低于初值且单调驱动下最大原理支持，
但我们不作全情景公理，直接在求解的每个接受步数值扫描 $\max_q U$——
全部时刻最大值均位于中心列（容差 $10^{-12}$），判据实现为"中心首次低于 0.15"。
阈值 0.15 干基 $=0.15/1.15=13.04\%$ 湿基，与药典对多数药材水分限度的惯例
（约 13\%）一致~\cite{pharmacopoeia}（解释性论述，见 \S\ref{sec:结果}）。

数值上报告\textbf{穿越区间加右侧安全端点}：问题 3 为 $[205837.7,\,205847.7]$ s，
$t_f=57.1799$ h；问题 4 为 $[182948,\,182963]$ s，$t_f=50.8230$ h。
四位小数舍入后的 60 s 网格判定为 57.2333 h，与未舍入值差 0.053 h（$\le0.2$ h，自洽）；
表中出现的 0.1500 是舍入显示，判定位一律用未舍入值。

\subsection{严格下界（排除性闸门）}
由比较原理：取 $D$ 的路径点态最大 $D_{\max}$（$D$ 对 $C$、$T$ 均单调增，
$D_{\max}=D(C_0,\max(T_0,\max T_a))$），常系数 Robin 参考问题干燥最快，
故 $t_f$ 必大于参考问题的达标时刻；其首模形式为
\begin{equation}
t_f\ge \frac{R^2}{D_{\max}\lambda_1^2}\,
\ln\!\frac{A_1\,(C_0-C_e)}{C_{\mathrm{th}}-C_e},
\label{eq:下界}
\end{equation}
其中 $\lambda_1$ 为 $\lambda J_1(\lambda)=Bi_m J_0(\lambda)$ 第一根（圆柱 Robin
特征值问题，标准谱理论见~\cite{crank}），
$A_1=2Bi_m^2/(\lambda_1J_1(\lambda_1)(Bi_m^2+\lambda_1^2))$ 为中心响应系数。
问题 3：$D_{\max}=1.359\times10^{-8}$ m$^2$/s、$Bi_m=1.177$，得 $t_f\ge 15.70$ h
（300 模精确级数穿越同为 15.7021 h，穿越时第二模占比 $5.7\times10^{-14}$，
首模占优严格成立）；膜界 $R/(2h_m)\ln[(C_0-C_e)/(C_{\mathrm{th}}-C_e)]=11.17$ h
（忽略内部扩散阻力，系统性偏弱，仅作次级参照）。
问题 4：沿真实 $R(s)$ 轨迹的时间积分版下界为 $19.02$ h
（$R_{\min}$ 常值版 15.95 h、固定 $R_0$ 版 37.61 h）。
两个主值（57.18 h、50.82 h）均远大于各自下界，闸门通过。
图~\ref{fig:穿越} 把下界画在达标穿越曲线上作为"低于此必错"的排除线。

\section{算法与实现}\label{sec:算法}

\textbf{空间}：式~\eqref{eq:质量坐标} 的固定 $q$ 网格单元中心有限体积（N=160 主网格），内面通量共享；
$q=0$ 面通量系数为零（正则对称自动满足）；表面用\textbf{半单元内阻与外对流阻串联}
——表面值 $U_s,\varTheta_s$ 由
$8D_{\mathrm{eff}}(U_s-U_N)/(R^2\Delta q)=2h_m(C_{\mathrm{env}}-U_s)/R$ 联立重建
（$D_{\mathrm{eff}}$ 取半单元中点值，强变的 $D\propto\mathrm{e}^{-0.45/C}$ 下若用端点值
会产生"内通量被人为归零"的伪根），绝不把末单元平均当表面值。

\textbf{时间}：后向欧拉加局部外推（一步对两半步，接受 $2\times$半对$-$整步，二阶），
外推态的守恒通量由各后向欧拉恒等式线性组合给出，仍严格守恒；
自适应步长（rtol $=10^{-7}$，一步对两半步误差比控制，失败缩步不截断负值），
尾段步长约 10--15 s，输出 1 s / 60 s 网格由步记录线性插值重构
（插值误差实测折 $2.6\times10^{-4}$ h，见 \S\ref{sec:误差}）。

\textbf{非线性}：外场 Gauss--Seidel（热场在冻结系数下线性求解，湿场在冻结温度下
Newton/Picard），湿场 Picard 起步、$\max_qU<0.6$ 后切换含 $\partial D/\partial C$ 的
Newton（尾段 $D$ 变化 16.8 倍；全程 Picard 对照 $\Delta t_f=1.7\times10^{-10}$ h，无感）。
耦合收敛判据 $\Delta U<10^{-10}$、$\Delta\varTheta<10^{-8}$ K。

\textbf{复现}：\texttt{python run\_all.py} 一条命令产出 result1--4 与全部门禁、
敏感性、结果总账（代码结构见附录~\ref{app:代码}），无人工干预；全部交付文件确定性写出（固定归档时间戳），
\textbf{连续两次运行 result1--4 的 md5 完全一致}（result1 \texttt{22eb47f7…}、
result2 \texttt{12cde4e8…}、result3 \texttt{fbe982ff…}、result4 \texttt{db8b34a8…}）。
全流程约 31 min。

\section{独立验证}\label{sec:验证}
可靠性来自\textbf{两种完全不同数学结构的互证}，而不是同一算法换网格。
六层验证体系见表~\ref{tab:验证}，每层写明能排除什么、不能排除什么。

\begin{table}[htbp]
\centering
\caption{验证汇总表（V1--V6）：手段、数学结构、实测数字、能/不能排除。}
\label{tab:验证}
\small
\begin{tabular}{p{0.30\linewidth}p{0.30\linewidth}p{0.32\linewidth}}
\toprule
层（手段） & 实测数字 & 能排除 / 不能排除 \\
\midrule
V1 问题 1 热场解析解（圆柱 Robin 特征展开 + 时变环境 Duhamel 卷积，谱解）vs 有限体积数值解 &
单元中心逐点最大偏差 $5.9\times10^{-7}\,^{\circ}$C（N=320）；收敛阶 2.00/1.99/1.99 &
排除离散、边界处理、时间积分错误（两种完全不同方法）；不能排除物性取值错误 \\
V2 等温线性化 Bessel 首模解 vs 全耦合尾段 &
尾段 $\ln(U_{\max}-C_e)$ 线性 $R^2=0.9972$；衰减率 $\gamma=3.55\times10^{-6}$/s 对应
等效 $D=2.54\times10^{-10}$（$C\approx0.108$，在尾段剖面区间内） &
排除几何与湿边界离散错误（尾段结构）；不能排除非线性段物性细节 \\
V3 严格下界闸门（比较原理，与离散无关） &
$t_f=57.18$ h $>$ 15.70 h；$t_f=50.82$ h $>$ 19.02 h &
排除错误的达标时间（低于下界必错）；不能给出上界 \\
V4 守恒恒等式（总水量、总焓，逐步对账） &
水量窗口相对残差 $\le5.5\times10^{-11}$；总焓全局吞吐残差 $\le1.1\times10^{-11}$ &
排除实现错误（一致性，曾两次抓到实现 bug）；不是独立验证，不能证明物理正确 \\
V5 退化测试（移动域 $R(t)\equiv R_0$；$D$ 冻结） &
移动域退化为固定域：$\Delta t_f=0.00$ s、场差 0；$D$ 冻结退回 Bessel 首模：偏差
$3.1\times10^{-9}$、首模商 0.99996 &
排除移动域实现错误；不能排除闭合假设（等比收缩）本身 \\
V6 网格/时间 Richardson 收敛 &
场级空间阶 1.90--2.45；$t_f$ 序列 57.249/57.201/57.180 h，外推偏差 0.016 h；
rtol/10 实测 $\Delta t_f=0.0014$ h &
只证明离散收敛（一致性）；\textbf{不能单独充当可靠性证据} \\
\bottomrule
\end{tabular}
\end{table}

两点纪律：\textbf{(i)} V1 与 V2 是谱解/解析结构对有限体积的\emph{真独立}对拍
（不是换参数、不是换网格；剖面对拍见图~\ref{fig:对拍}），问题 1 的解析解还使用稳态减法消除级数尾巴，
使解析参考自身误差 $<10^{-8}\,^{\circ}$C；\textbf{(ii)} V4/V6 属于一致性检查，
只能证明实现与离散无误，本文不把它们说成可靠性证明。

\begin{figure}[htbp]
\centering
\includefig{fig2_问题1解析对拍.pdf}{问题 1 解析对拍}
\caption{问题 1 的解析对拍（回答"可靠性如何证明"）：代表时刻（100/600/1800 s）
径向温度剖面，圆柱 Robin 特征展开解析解（线）vs 守恒有限体积数值解（点）；
单元中心逐点最大偏差 $5.9\times10^{-7}\,^{\circ}$C（N=320），插图为偏差随网格的
收敛（实测阶 2.00）。两种完全不同的数学结构互证，排除离散、边界与时间积分错误。}
\label{fig:对拍}
\end{figure}
\section{结果与解释}\label{sec:结果}

\subsection{问题 1：预热平衡阶段}
图~\ref{fig:输入} 给出附件 1 的环境输入及其口径处理。表~\ref{tab:表1}、表~\ref{tab:表2}
给出 7 个时刻 5 个位置的温度与水分浓度（result1.xlsx 含 1 s $\times$ 0.1 cm 全场）。
温度在 1800 s 内明显传向中心（中心列 28.0001 $\to$ 33.5753 ${}^\circ$C，
与解析解四位小数一致）；水分渗透深度约 3 mm $\ll R$，中心列保持 2.5500，
表面列降至 1.5102 kg/kg——两场的空间尺度差 $Le\sim O(10^2)$ 的直接表现。

\begin{figure}[htbp]
\centering
\includefig[\linewidth]{fig1_输入数据.pdf}{输入数据}
\caption{输入数据与口径（回答"数据可用性如何裁定"）：附件 1 的温度与空气水分浓度
（241 点、60 s 步、终于 4 h；标出 4 h 截止与末值保持延拓 $T_a=50.165\,^{\circ}$C、
$C_{\mathrm{env}}=0.04986$）；附件 2 外半径（145 点、1800 s 步，2.000→1.198 cm；
标出约 21 h 冻结与平台延拓）。读法：两条数据曲线都只覆盖过程的一小段，
外推口径的选择是敏感性分析的对象（\S\ref{sec:敏感}）。}
\label{fig:输入}
\end{figure}

\subsection{问题 2：整个烘干过程}
表~\ref{tab:表3}、表~\ref{tab:表4} 给出前 3 h 的场（result2.xlsx 含 1 s 全场至
$t_f+3600$ s）。双向耦合的效应清晰可见：温度升高使 $D\propto\mathrm{e}^{-3850/T}$
增大约 2.4 倍，干燥在升温段（前约 10 h）显著加速；湿分下降又通过
$\rho c_p(C)$、$k(C)$ 反作用于热场。图~\ref{fig:剖面} 给出代表时刻的径向剖面演化。

\begin{figure}[htbp]
\centering
\includefig[\linewidth]{fig3_温湿剖面.pdf}{温湿剖面演化}
\caption{温湿剖面演化（回答"场长什么样"）：问题 2/3 的 $C(r,t)$ 与 $T(r,t)$
在代表时刻的径向剖面；可见中心最慢（全部时刻最大值在中心，容差 $10^{-12}$）、
尾段因 $D(C)$ 骤降（最多 16.8 倍）形成低扩散率干燥外壳而拖长。}
\label{fig:剖面}
\end{figure}

\subsection{问题 3：达标时间}
表~\ref{tab:表5} 给出每 6 h 的水分浓度。达标时间（判据 \eqref{eq:达标}）：
\textbf{$t_f=57.1799$ h}（穿越区间 $[205837.7,205847.7]$ s，插值 57.1795 h；
收敛序列 N=40/80/160 = 57.249/57.201/57.180 h，Richardson 外推 57.1636 h，
主值偏差 0.016 h 即空间误差预算；式~\eqref{eq:下界}的严格下界 15.70 h 通过）。
尾段为何拖长：随表面变干，$D\propto\mathrm{e}^{-0.45/C}$ 最多缩小 16.8 倍，
形成低扩散率的干燥"外壳"，中心（最慢点）的残余水分只能穿过它排出——
尾段 $\ln(U_{\max}-C_e)$ 呈指数衰减（$R^2=0.9972$），衰减率对应剖面加权等效
$D\approx2.5\times10^{-10}$ m$^2$/s（V2，见表~\ref{tab:验证}）。
阈值 0.15 干基 $=13.04\%$ 湿基，与药典对多数药材水分限度的惯例（约 13\%）一致，
故该判据有明确的药学含义：安全贮藏水分。

\begin{figure}[htbp]
\centering
\includefig{fig4_达标穿越.pdf}{达标穿越}
\caption{达标穿越（回答"$t_f$ 多少、收缩贡献多大"）：$\max_qU(q,t)$ 全程曲线与
0.15 阈值线；问题 3 主解（$t_f=57.1799$ h）、问题 4 主解（$t_f=50.8230$ h）与
附录 4 固定半径对照（129.1047 h）三线对比，横轴标出复算严格下界
（15.70 h / 19.02 h，低于此必错）。一图同时交付归因、判据与下界闸门。}
\label{fig:穿越}
\end{figure}

\subsection{问题 4：收缩移动域}
表~\ref{tab:表6} 给出每 6 h 的水分浓度（超出当前表面的固定列留空）。
达标时间 \textbf{$t_f=50.8230$ h}（穿越区间 $[182948,182963]$ s；收敛序列
50.838/50.830/50.823 h，Richardson 外推 50.7999 h；收缩下界 19.02 h 通过）。
\textbf{与问题 3 的大小关系必须给机制解释}（三组对照，表~\ref{tab:验证}的 V5
保证移动域实现无误）：
\begin{itemize}
  \item \textbf{公式效应}：附录 4 的 $D$ 在同状态下约为附录 3 的 $1/5.4$
        （$2.5\times10^{-9}$ vs $1.36\times10^{-8}$ m$^2$/s）；
        固定半径下 $t_f$ 从 57.18 h 放大到 129.10 h（$\times2.26$；非 $\times5.4$，
        因 $t_f$ 对 $D$ 是近线性加对数修正的依赖，且 $Bi_m$ 从 1.18 升到 6.36，
        干燥从膜-扩散共同限制转为扩散主导）。
  \item \textbf{收缩效应}：$R^2$ 降 64\%（扩散时间尺度 $\propto R^2$ 缩至 0.36）、
        $2/R$ 换热换质面积比增 67\%——把 129.10 h 拉回 50.82 h（$-60.6\%$），
        收缩是本问题最大的单点加速因素。
  \item \textbf{净效应}：公式减速（$\times2.26$）被收缩加速（$\times0.39$）反超，
        问题 4 反而比问题 3 快 $11.1\%$。
\end{itemize}

收缩停止的一致性问题（附件 2 在约 21 h 起冻结于 1.198 cm 平台，比达标早约 15.8 h）
的完整定量分析见下一小节：附件 2 并非单一失水收缩曲线，我们用第一性原理的内生
闭合对它做了结构性复现与偏差分解。

\subsection{收缩机制分析：为什么单一失水收缩不可能}\label{sec:收缩机制}
本小节与下一小节是讨论/验证性质的分析：\textbf{主模型仍以附件 2 为外生几何}
（题目"请根据附件 2"的直接口径），以下内容不改变 result4 与任何交付数字。

\textbf{三段复合结构}：附件 2 的收缩曲线不是单一失水收缩——A 急缩段
（0--3.5 h，占全程收缩的 61\%）、B 缓缩段（3.5--21 h）、C 冻结段（约 21 h 起，
平台 1.198 cm）。

\textbf{滞后交叉诊断}：在失水等容闭合 $\bar U_{\mathrm{inf}}=(1+C_0)(R/R_0)^2-1$ 下
反推附件 2 的等效含水率，与模型平均含水率 $\bar U_{\mathrm{model}}$ 在
\textbf{约 16 h 交叉}（图~\ref{fig:收缩机制}）——早期附件 2 更干、后期更湿
（35 h 冻结点：反推 $\bar U=0.278$ 对模型同刻的 $0.149$）。
\textbf{因此任何单一单调的 $R(\bar U)$ 映射在数学上都不可能复现附件 2}
（失水等容收缩只是其中一条单调曲线）；复现该交叉必须有两组独立机制
（早期力学压实 + 后期玻璃化成孔，成孔缺口 $0.129\to0.155$ kg/kg，
对应附件 2 隐含的末期孔隙率约 $31\%$）。零拟合的"理想收缩+玻璃化冻结"闭合
（$C_{\mathrm{glass}}\approx0.21$，Gordon--Taylor 反解/文献值~\cite{saravacos}）
已能结构性给出减速与平台（平台半径偏差 $-2.4\%$、冻结时刻偏差 $+12\%$），
且附件 2 的冻结点（约 21 h）恰好对应模型平均含水率降到 $C_{\mathrm{glass}}$
的时刻（$\bar U(21\ \mathrm{h})=0.234$）——这把机制链条推进到"定量可标定"，
即下一小节的内生收缩模型。

\subsection{内生收缩模型：推导、参数估计与验证}\label{sec:内生收缩}

\textbf{第一步：机制推导（第一性原理）}。收缩由三项第一性原理机制叠加，
一项一机制、总参数 5 个：
\begin{equation}
R(t)=\underbrace{R_0\sqrt{\frac{1+\bar U(t)}{1+C_0}}}_{\text{失水收缩}}\cdot
\underbrace{\big[1-\varepsilon_c\,g_c(\bar U)\big]}_{\text{毛细-膨压塌陷}}\cdot
\underbrace{\big[1+\varepsilon_p\,g_p(C_s)\big]}_{\text{结皮-成孔停滞}}
\label{eq:收缩闭合}
\end{equation}
(i) \textbf{失水收缩}（零参数基线）：水分守恒 + 固体/水等密度声明，体积随失水减少；
(ii) \textbf{毛细-膨压塌陷}：干燥初期毛细吸力 $2\gamma/r_{\mathrm{pore}}$ 压密基体、
膨压随失水丧失，应变先发展后被成孔回弹，
$g_c(x)=(1-x)^{a}x^{b}$（$x=(C_0-\bar U)/C_0$ 为失水率，峰值归一）——
其应变量级有独立的毛细压实估计支撑（bracket $[0.028,0.580]$ cm 覆盖观测超额
0.182 cm @2 h、半径应变 9.1\%）；
(iii) \textbf{结皮-成孔停滞}：表面 $C_s$ 穿越玻璃化转变含水率
$C_{\mathrm{glass}}=0.21$（文献值，非拟合）后收缩停滞、继续失水转化为孔隙，
$g_p=\mathrm{clip}\big((C_{\mathrm{glass}}-C_s)/(C_{\mathrm{glass}}-C_{s,\min}),0,1\big)^{k_p}$。
两项修正均\textbf{由湿度场驱动而不显含时间}——这是关键结构选择：
时间驱动的替代闭合在耦合内生时会把湿度场的相位误差搬进 $R(t)$
（实测其耦合 $t_f$ 偏 $+3.0$ h），湿度驱动无此问题。

\textbf{第二步：参数估计（诚实标注）}。表~\ref{tab:收缩参数} 列出全部参数：
5 个标定参数由最小二乘对附件 2 估计（不是由任何公式"算出"），
2 个量取文献值不参与拟合；标定流程与留出法验证细节见附录~\ref{app:内生}。

\begin{table}[htbp]
\centering
\caption{内生收缩模型参数表（拟合/非拟合分栏诚实标注）与物理合理性。}
\label{tab:收缩参数}
\small
\begin{tabular}{llll}
\toprule
参数 & 数值 & 来源 & 物理合理性核对 \\
\midrule
$\varepsilon_c$（塌陷峰值应变） & 0.1108 & 拟合附件 2 & 11.1\% 在毛细压实区间 $[1.4\%,29\%]$ 内 \\
$a,\ b$（塌陷形状指数） & 1.668,\ 1.220 & 拟合附件 2 & 峰值相位 $\bar U\approx1.47$（约 4 h）与急缩段一致 \\
$\varepsilon_p$（停滞幅度） & 0.0709 & 拟合附件 2 & 对应末期孔隙率 30.9\% $\approx$ 附件 2 隐含 31.0\% \\
$k_p$（停滞开启陡峭度） & 3.787 & 拟合附件 2 & 开启相位由 $C_s$ 穿越 $C_{\mathrm{glass}}$ 物理锚定 \\
$C_{\mathrm{glass}}$（玻璃化含水率） & 0.21 & \textbf{文献}（Gordon--Taylor），未拟合 & 文献区间 0.19--0.23 内 \\
$\rho_{sk}$（骨架密度，kg/m$^3$） & 1468 & \textbf{文献}，未拟合 & 生物质骨架密度典型范围 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{第三步：对附件 2 的验证}（图~\ref{fig:内生贴合}）。拟合优度：
RMSE $=0.047$ mm（附件 2 量化分辨率 0.01 mm 的约 5 倍）、最大偏差 0.162 mm；
比率 $m=R/R_{\mathrm{ideal}}$ 的深坑（0.889 @ 4 h，数据 0.894）、
\textbf{滞后交叉（预测 17.0 h，数据约 16 h）}、平台（1.1969 vs 1.198 cm，$-0.09\%$）、
三段形态全部复现。过拟合自检（奇偶折半留出法）：训练 0.045--0.050 mm、
留出 0.045--0.050 mm，两折参数稳定——无过拟合；残差 lag-1 自相关 0.82、
尚有 $\pm0.16$ mm 级低频形状（B 段过渡区的弱欠拟合），已贴近数据分辨率极限，
不再加参数。体积闭合 $V=V_s+V_w+V_{\mathrm{pore}}$ 逐点自洽：$V_{\mathrm{pore}}$
早期为负（基体压密=塌陷的体积解释）、9.5 h 转正后单调（成孔）、
末期孔隙率 30.9\%。

\textbf{关键一致性}：把收缩完全内生——$R$ 由湿度场逐步预测（每步以当前
$\bar U$、$C_s$ 代入式~\eqref{eq:收缩闭合}）、不读附件 2——重解问题 4 得
\textbf{$t_f=50.72$ h，与主值 50.82 h 一致（$-0.22\%$）}；单向替换
$R_{\mathrm{pred}}(t)$ 重解得 50.88 h（$+0.10\%$）；耦合版 $R(t)$ 与附件 2 的
RMSE 仍为 0.048 mm、末期半径 1.1967 cm。

\textbf{结论}：附件 2 的收缩可由三项第一性原理机制叠加内生复现
（0.05 mm 量级、量化分辨率的 5 倍以内），且给出相同的达标时间；
滞后交叉由"塌陷先发展、成孔后开启"的两机制相位差自然产生。
\textbf{主结果 result4 仍用附件 2 实测半径}（题给数据）——内生模型给出相同的
$t_f$（50.72 vs 50.82 h，0.2\%），证明收缩机制被正确捕获；
它同时是附件 2 数据质量的独立佐证。

\begin{figure}[htbp]
\centering
\includefig[\linewidth]{fig7_内生收缩贴合.pdf}{内生收缩贴合}
\caption{内生收缩模型对附件 2 的贴合与验证（回答"收缩能否由模型内生预测"）：
三项第一性原理机制叠加的闭合（式~\eqref{eq:收缩闭合}，5 个标定参数 +
2 个文献量）几乎完美复现附件 2——RMSE 0.047 mm（量化分辨率的 5 倍）、
滞后交叉（17.0 h vs 约 16 h）、平台（1.1969 vs 1.198 cm）、三段形态全对；
残差子图显示偏差有界在 $\pm0.16$ mm。}
\label{fig:内生贴合}
\end{figure}

\begin{figure}[htbp]
\centering
\includefig[\linewidth]{fig6_收缩机制分析.pdf}{收缩机制分析}
\caption{收缩机制分析（回答"附件 2 是什么样的收缩"）：附件 2 的实测外半径
不是单一失水收缩曲线（A 急缩 61\%/B 缓缩/C 平台三段 + 16 h 滞后交叉）；
零拟合的第一性原理闭合（理想收缩+玻璃化冻结等）结构性复现其减速与平台
（平台半径偏差 $-2.4\%$），早期塌陷与后期成孔分别对应两组独立物理机制。
数据源：附件 2 与问题 4 主解湿度场。}
\label{fig:收缩机制}
\end{figure}

\input{tables}

\subsection{敏感性分析}\label{sec:敏感}
图~\ref{fig:敏感} 与表~\ref{tab:敏感性} 给出每组实测 $\Delta t_f$（不只报方向）。

\begin{table}[htbp]
\centering
\caption{敏感性矩阵（问题 3 设定、N=80 对照档；问题 4 拆分用主档）。}
\label{tab:敏感性}
\small
\begin{tabular}{lll}
\toprule
组别 & $t_f$ (h) & $\Delta t_f$ (h) \\
\midrule
$C_e=0.04986$（题目口径，主） & 57.2009 & — \\
$C_e=0.1366$（山药解吸支，\textbf{题外修正}） & 79.8270 & $+22.6261$（$+39.6\%$） \\
环境外推：末值保持（主） & 57.2009 & — \\
环境外推：线性外推（末 1 h 斜率） & 58.8203 & $+1.6194$ \\
环境外推：末 1 h 均值 & 57.5034 & $+0.3025$ \\
半径插值：PCHIP（主，问题 4） & 50.8295 & — \\
半径插值：线性（问题 4） & 50.8317 & $+0.0022$ \\
问题 4 拆分：附录 4 固定 $R$ & 129.1047 & $+78.28$（对主解） \\
问题 4 拆分：附录 4 $+R(t)$（主解） & 50.8230 & — \\
问题 4 拆分：问题 3 & 57.1799 & $+6.36$（对主解） \\
\bottomrule
\end{tabular}
\end{table}

边界汇口径（$C_e$）是本题\emph{唯一能量级翻盘的单点}：改用解吸等温线
$C_e=0.1366$ 后 $t_f$ 增大 $22.6$ h（$39.6\%$）。按纪律，主结果一律用题目口径，
题外修正只入敏感性并明确标注；该偏移\textbf{不进}误差预算（\S\ref{sec:误差}）。
半径插值两档实测仅差 $0.0022$ h（附件 2 平滑，两插值几乎重合），该口径不敏感。

\begin{figure}[htbp]
\centering
\includefig[\linewidth]{fig5_敏感性.pdf}{敏感性}
\caption{敏感性条形图（回答"结论有多稳"）：各因素 $\Delta t_f$ 横向条形——
边界汇口径 $C_e=0.1366$（题外修正）$+22.63$ h 自然主导，环境外推 $+1.62$ h 次之，
半径插值仅 $+0.002$ h；诚实展示最大不确定性来源，主结果一律用题目口径。}
\label{fig:敏感}
\end{figure}

\section{误差与不确定度}\label{sec:误差}
误差预算由\textbf{可指认的误差源}逐项给出量级并确定性合成（三角不等式），
不冒充统计区间（表~\ref{tab:预算}）；区分三类：数值误差（可收敛）、口径/固有（不可消除但可指认）、
情景假设（环境外推）。

\begin{table}[htbp]
\centering
\caption{误差预算表（合成区间不含 $C_e$ 口径，后者单列敏感性）。}
\label{tab:预算}
\small
\begin{tabular}{lll}
\toprule
误差源 & 量级 (h) & 类型 \\
\midrule
空间网格（N=160，Richardson 偏差） & 0.0164 & 可收敛 \\
时间步（rtol/10 实测） & 0.0014 & 可收敛 \\
非线性迭代（Picard/Newton 对照） & $1\times10^{-6}$ & 可收敛 \\
输出重建 / 1 s 插值（曲率上界实测） & $2.6\times10^{-4}$ & 可收敛 \\
半径插值（线性 vs PCHIP，问题 4） & 0.0022 & 口径依赖 \\
舍入（60 s 网格 + 四位小数实测） & 0.0701 & 固有 \\
环境外推（三档最大偏移） & 1.6194 & 情景假设 \\
\midrule
\textbf{确定性合成（三角不等式）} & $\pm1.71$ & 不含 $C_e$ 口径 \\
\bottomrule
\end{tabular}
\end{table}

故问题 3 的达标时间报告为 $t_f=57.18$ h $\pm1.71$ h（合成区间内数值误差仅占
0.02 h 量级，主导项是环境外推口径）。$C_e$ 偏移（$+22.6$ h）不进区间：
若区间一半以上宽度来自一个题外且不可辨识的参数，等于把口径选择包装成不确定度；
本文的处理是先给题目口径主值，再把 $C_e$ 单列敏感性（\S\ref{sec:敏感}）。

\section{结论与局限}\label{sec:结论}
\begin{enumerate}
  \item \textbf{题目口径的结论}（每条紧跟关键数值）：四问统一模型可解、可验证、可复现。
        问题 1 的场与解析解逐点一致——最大偏差 $5.9\times10^{-7}\,^{\circ}$C、
        收敛阶 2.00（图~\ref{fig:对拍}），即离散、边界与时间积分三类实现错误被排除；
        问题 3 达标时间 \textbf{$t_f=57.1799$ h $\pm1.71$ h}（图~\ref{fig:穿越}；
        区间由七源误差预算确定性合成，见表~\ref{tab:预算}）；
        问题 4 达标时间 \textbf{$t_f=50.8230$ h}——$t_4<t_3$ 的机制是
        "附录 4 扩散系数更小（$\times2.26$）被半径收缩（$-60.6\%$）反超（净 $-11.1\%$）"
        （图~\ref{fig:穿越} 三线对照：57.18 / 50.82 / 129.10 h）。
        一句话解读：本题干燥全程由内部扩散主导（$Bi_m$ 判据，集总会偏 $-46.0$ h），
        收缩是问题 4 的最大加速因素，而环境温度 4 h 后的口径是第二大不确定来源。
  \item \textbf{依赖声明假设的结论}：湿边界有效闭合（$C_{\mathrm{env}}$ 直接作驱动势）
        是最大单点依赖（改用等温线则 $t_f=79.83$ h，$+39.6\%$，图~\ref{fig:敏感}）；
        环境 4 h 后末值保持外推（三档差 $+1.62$ h 以内）；
        附件 2 的约 21 h 冻结（1.198 cm 平台）与延拓——内生收缩模型
        （\S\ref{sec:内生收缩}，图~\ref{fig:内生贴合}）以三项第一性原理机制复现附件 2
        （RMSE 0.047 mm），耦合内生重解给出相同达标时间
        \textbf{（50.72 vs 50.82 h，偏差 $-0.22\%$）}：
        收缩被内生预测且与实测一致——一句话解读：题给的半径曲线与湿度场所蕴含的
        收缩互为印证，附件 2 的数据质量与主模型互相独立佐证；
        问题 4 的内部等比收缩（附件 2 只识别外半径，内部收缩不可识别——
        这是本题的固有局限，本文以退化测试与效应拆分控制其影响）。
  \item \textbf{可靠性证据的强度}：结论由两种不同数学结构（解析谱解/首模解 vs
        守恒有限体积）互证，叠加比较原理下界（15.70/19.02 h）与守恒恒等式门禁
        （残差 $\le5.5\times10^{-11}$）；网格收敛仅作一致性检查，未单独充当验证。
\end{enumerate}
\begin{thebibliography}{9}
\bibitem{luikov} Luikov A V. \emph{Heat and Mass Transfer in Capillary-Porous Bodies}.
Pergamon Press, Oxford, 1966.
\bibitem{crank} Crank J. \emph{The Mathematics of Diffusion}. 2nd ed.,
Oxford University Press, 1975.
\bibitem{pharmacopoeia} 国家药典委员会. \emph{中华人民共和国药典（一部）}.
北京: 中国医药科技出版社, 2020.（通则 0212《药材和饮片检定通则》：多数药材水分限度约 13\%）
\bibitem{saravacos} Saravacos G D. Mass transfer properties of foods. In:
\emph{Engineering Properties of Foods} (M. A. Rao, S. S. H. Rizvi, eds.),
Marcel Dekker, New York, 1986.
\end{thebibliography}

\appendix
\section{移动域方程的推导要点}\label{app:移动域}
取材料坐标 $q$（干物质标签），物理坐标 $r=\psi(q,t)$。水分守恒在物理坐标为
$U_t|_r=(1/r)\partial_r(rD\,\partial_r U)$。链式法则给出
$U_t|_r=U_t|_q-\psi_t U_q/\psi_q$ 与
$(1/r)\partial_r(rD U_r)=(1/(r\psi_q))\partial_q(rD U_q/\psi_q)$。
对仿射映射 $\psi=R(t)\sqrt q$（长度不变、内部等比），干固体守恒
$\partial_t(\rho_s r\psi_q)+\partial_q(\rho_s r\psi_t\cdot 0)$ 的可积性要求
任意截面两侧的干物质量不随时间改变，即材料坐标随体运动。
代入得迁移项 $\psi_t U_q/\psi_q$ 与通量展开中的对应项\textbf{精确相消}，方程化为
$U_t|_q=\partial_q(4qD/R(t)^2\,U_q)$，与固定域同形，无伪对流项。
在移动表面 $q=1$ 处，对表面薄层做水分衡算，$-\dot R U_s$ 项与通量跳条件中的
同名片精确相消，Robin 边界 $-D\partial_rU=h_m(U_s-C_{\mathrm{env}})$ 形式不变。
温度方程同理。

\section{复现清单与代码结构}\label{app:代码}
一条命令 \texttt{python run\_all.py}（Windows 下 \texttt{PYTHONIOENCODING=utf-8}，
依赖仅 numpy/scipy/pandas/openpyxl）依次执行：
\begin{itemize}
  \item \texttt{run\_q1.py}：问题 1 求解 + V1 解析对拍 + 守恒/退化门禁 + result1.xlsx；
  \item \texttt{run\_q23.py}：问题 2/3 耦合求解 + $t_f$ + V3/V4/V6 门禁 +
        result2/result3.xlsx + 环境外推三档；
  \item \texttt{run\_q4.py}：问题 4 移动域 + T6 效应拆分 + V5 退化 + T7 一致性 + result4.xlsx；
  \item \texttt{run\_m4.py}：判据族反验 + 敏感性矩阵 + 误差预算 + V2 尾段 + 结果总账。
\end{itemize}
核心求解器：\texttt{solver\_q1.py}（质量坐标 FV、谱分解精确时间积分）、
\texttt{analytic\_heat.py}（Robin 特征展开 + Duhamel 卷积 + 稳态减法）、
\texttt{solver\_q23.py}（双向耦合 BE+局部外推+Picard/Newton）、
\texttt{solver\_q4.py}（仿射收缩移动域，物性包与 $R(t)$ 参数注入）。
全部运行日志与逐层数据落 \texttt{03-数据/}（含 \texttt{结果总账.csv} 272 行，
为摘要、正文、表格、图注数字的唯一来源）。

\section{量纲检查表}\label{app:量纲}
\begin{table}[htbp]
\centering
\caption{量纲门禁（逐条通过；$T$ 的开尔文换算与 $h_m$/$D$ 共轭关系单列）。}
\label{tab:量纲}
\small
\begin{tabular}{lll}
\toprule
符号 & 单位 & 核对要点 \\
\midrule
$\rho,c_p,k$ & kg/m$^3$、J/(kg$\cdot$K)、W/(m$\cdot$K) &
$a_{\mathrm{eff}}=\rho c_p$ 为 J/(m$^3\cdot$K)，与 $\partial_q(4qk/R^2\varTheta_q)$ 一致 \\
$D$ & m$^2$/s & $4qD/R^2$ 为 1/s，与 $U_t$ 一致 \\
$T$ & K（不是 ${}^\circ$C） & $\mathrm{e}^{-3850/T}$ 的 3850 单位为 K；
读入 assert($-50<T_a(^{\circ}\mathrm{C})<150$)、全程 assert($200<T_K<700$) \\
$h,h_m$ & W/(m$^2\cdot$K)、m/s &
$-k\partial_r\varTheta=h(\varTheta_s-T_a)$ 两侧 W/m$^2$；
$-D\partial_rC=h_m(C_s-C_{\mathrm{env}})$ 两侧 m/s \\
$h_m$ 与 $D$ 共轭 & — & 同一 Robin 结构 $Bi_m=h_mR/D$；
表面湿通量 $F=(2h_m/R)(C_{\mathrm{env}}-U_s)$ 为 1/s，与 $d\bar U/dt$ 一致 \\
$C,C_{\mathrm{env}}$ & kg/kg & $C_{\mathrm{env}}$ 按有效闭合直接代入，阈值 0.15 同基准 \\
$q,r$ & 无量纲、m & $r=R\sqrt q$；通量系数 $4qD/R^2$ 为 1/s \\
\bottomrule
\end{tabular}
\end{table}

\section{内生收缩模型的标定流程}\label{app:内生}
内生收缩模型（式~\eqref{eq:收缩闭合}）的 5 个参数
（$\varepsilon_c,a,b,\varepsilon_p,k_p$）由 scipy 的 \texttt{least\_squares}
在附件 2 的 145 个采样点上对 $R_{\mathrm{pred}}-R_{\mathrm{data}}$ 做带物理边界约束的
最小二乘估计（边界取物理合理区间，如 $\varepsilon_c\in[0.01,0.6]$）；
$C_{\mathrm{glass}}=0.21$ 与 $\rho_{sk}=1468$ kg/m$^3$ 取文献值、不参与拟合。
过拟合自检用奇偶折半留出法：两折上训练与留出 RMSE 均在 0.045--0.050 mm
（训练≈留出），且两折参数互差 $<10\%$，判定无过拟合。
标定与验证的完整复现脚本为 \texttt{08-临时/endogenous\_calibrated.py}
（约 70 s，含单向替换与耦合内生两次问题 4 重解），数据落
\texttt{03-数据/endogenous\_calibrated.csv} 与 \texttt{endogenous\_coupled.csv}。
湿度场驱动量 $\bar U(t)$、$C_s(t)$ 取自问题 4 主解的步记录
（\texttt{03-数据/q4\_main\_steps.npz}）。

\end{document}
