# M5 写作说明（论文 LaTeX 化）

日期：2026-09-11（同日增补收缩机制小节）。产物：`main.tex`、`tables.tex`（`gen_tables.py`
由 `paper_tables.csv` 生成）、`main.pdf`（**19 页，6 张图全部嵌入**）、`check_numbers.py`
（数字一致性自查）、`fix_backslash.py`（一次性修复 heredoc 吞反斜杠的工具）。

## 1. 编译

- 命令：`xelatex main.tex`（TeX Live 2026，连跑 2–3 次解析交叉引用）。
- 结果：编译通过，无致命错误；`main.log` 中 **0 处 `??`、0 处 undefined reference、
  0 条 LaTeX Warning**；输出 19 页（竞赛正文规模 15–25 页内），嵌入 6 个图形对象。
- 图：`\graphicspath{{../05-图表/}}`，fig1–fig6 全部 `\includegraphics`（P5 要求 ≥4，实际 6 张全用）；
  每张图题写清"回答什么问题"（数据裁定/可靠性证明/场形态/达标与归因/结论稳健性/收缩机制）。

## 1.1 增补内容（2026-09-11 第二轮）

- **新增小节 §6.5"收缩机制分析"**（问题 4 小节之后、敏感性之前，嵌入
  `fig6_收缩机制分析.pdf`）：附件 2 三段复合结构（A 急缩 61%/B 缓缩/C 冻结平台 1.198 cm）；
  滞后交叉诊断（约 16 h 交叉 ⇒ 单一单调 R(Ū) 映射数学上不可能复现）；零拟合
  "理想收缩+玻璃化冻结"闭合结构复现减速+平台（平台半径 −2.4%、冻结时刻 +12%，
  附件 2 冻结点对应 Ū(21 h)=0.234≈C_glass）；偏差分解为早期毛细塌陷
  （0.182 cm、应变 9.1%、落在物理 bracket [0.028,0.580] cm）与后期成孔
  （0.129→0.155 kg/kg、隐含孔隙率约 31%）；诚实结论（完美贴合不可行/结构复现可行，
  主模型仍用附件 2 外生几何）。全部数字取自 `03-数据/内生收缩分析.md`，未改一字。
- **两条定量声明落位**：§2.3 闭合假设第 4 条（端面效应定量：R/L=8%、路径 6.25 倍、
  时间尺度 ~39 倍、贡献 ~1% 量级）与第 6 条（ρ(C) 不作质量/体积闭合：附录 3 会给出
  负含水率/需 R≥1.301 cm vs 数据 1.198 cm；ρ 只用于 a_eff=ρc_p 与通量算子 ρ_sv=ρ/(1+C)）。
- 全文"35 h 冻结"口径统一为"约 21 h 起冻结于 1.198 cm 平台"（与内生分析一致，共 4 处）。

## 1.2 第三轮：写作/排版规范优化（math-modeling 规范，2026-09-11）

- **P1 图宽**：`\includefig` 加可选宽度参数；fig1/fig3/fig5/fig6（6.3 in 全宽图）用
  `width=\linewidth`，fig2/fig4（5.4 in）保持 `0.86\linewidth`，逐张核对调用。
- **P2 图注**：六张图注全部补齐"回答什么问题 + 关键数字 + 怎么读"
  （如图 2 补 $5.9\times10^{-7}$ ℃ 与阶 2.00、图 4 补三个 $t_f$ 与两条下界、
  图 5 补 $C_e$ 主导 +22.63 h、图 6 补三段结构与数据源）。
- **P3 摘要四段式**：问题与挑战 → 方法 → 关键结果（带数值）→ 验证与创新；
  607 中文字（含数字 870 字符，`check_abstract.py` 实测），无公式图表；
  关键词改为"热湿耦合；质量坐标；守恒有限体积；判据族；独立验证；中药材干燥"
  （方法词 ≥2 + 领域词 ≥2）。
- **P4 结论**：每条结论紧跟具体数值与图引用（$t_f=57.1799\pm1.71$ h、$t_f=50.8230$ h、
  机制归因 ×2.26/−60.6%/−11.1%、$5.9\times10^{-7}$ ℃、+39.6%、−2.4%、
  守恒残差 ≤5.5e-11、下界 15.70/19.02 h）。
- **P5 引用与文献**：补齐缺失引用（tab：判据族、tab：预算、tab：量纲、fig：对拍、
  fig：收缩机制、eq：边界/质量坐标/下界、app：移动域/代码/量纲）；
  新增四处 `\cite`（Luikov 于模型节、Crank 于谱理论、药典于阈值含义、Saravacos 于玻璃化），
  `check_refs.py` 核对 label/ref/cite/bibitem 双向对应——全部通过。
- **P6 重编译**：xelatex 两遍通过，main.pdf 19→21 页，0 `??`、0 undefined、0 Warning，
  6 图嵌入。图表代理的图底文字清理不影响本侧（图注已自含关键信息）。
- 新增脚本：`check_refs.py`（引用双向核对）、`check_abstract.py`（摘要字数）；
  `check_numbers.py` 仍全部通过（本轮未改任何结果数字）。

## 1.3 第四轮：内生收缩模型作为一等成果（反写：推导→参数估计→验证）

- **§6.5 改名"收缩机制分析：为什么单一失水收缩不可能"**（保留 fig6 与滞后交叉诊断，
  补写成孔缺口 0.129→0.155 kg/kg / 隐含孔隙率 ~31%），作为内生模型的引子；
  **新增 §6.6"内生收缩模型：推导、参数估计与验证"**——严格按
  机制推导（三项第一性原理机制，式 (6.x)）→ 参数表（表：tab：收缩参数，
  5 拟合 + 2 文献分栏诚实标注）→ 验证（RMSE 0.047 mm、交叉 17.0 h、平台 −0.09%、
  留出法无过拟合、V_pore 压密→成孔、**耦合内生 t_f=50.72 h vs 主值 50.82 h（−0.22%）**）
  → 结论（主结果仍用附件 2，内生模型给出相同 t_f）。
- **诚实性红线落实**：标定流程（scipy least_squares、约束边界、留出法两折数字）
  写入附录 D（app：内生）；正文参数表逐参数标"拟合附件 2 / 文献未拟合"；
  无任何参数写成"由公式算出"。
- **fig7_内生收缩贴合.pdf** 嵌入（6.3 in 全宽，`width=\linewidth`），图注含
  RMSE/交叉/平台数字；fig6 保留作诊断引子。
- **摘要/结论呼应**：摘要"验证与创新"段加内生收缩句（RMSE 0.047 mm、50.72 vs 50.82 h）；
  结论第 2 条加"收缩被内生预测且与实测一致"（一图一数一句）。
- 编译：xelatex 两遍通过，main.pdf 21→**24 页**，7 图嵌入，0 `??`、0 undefined、0 Warning；
  摘要 684 中文字（988 含数字字符），仍在规范区间。
- `check_numbers.py` 新增 19 项内生收缩断言（含补写的 0.129/0.155/31%），全部通过；
  `check_refs.py` 通过（tab：收缩参数、fig：内生贴合、app：内生 均双向对应）。

## 1.4 第五轮：论文统一切换到内生几何口径（用户拍板，result4 已内生）

- **数字统一（问题 4 全族，问题 1/2/3 未动）**：
  t_f4 主值 50.8230 → **50.5369 h**（未舍入右端点，穿越区间 [181917.7,181932.7] s；
  收敛序列 50.5450/50.5370/50.5369，Richardson 50.5369）；
  附件 2 外生版 50.8230 h 降为"交叉验证"（内生−外生 −0.286 h，−0.56%）；
  效应拆分 −60.6%→**−60.9%**、−11.1%→**−11.6%**、拆分 Δ 78.28→78.57 / 6.36→6.64；
  §6.6 耦合 t_f 50.72→50.5369（生产档），内生几何自洽 RMSE 0.048→**0.074 mm**
  （如实写明回耦正常代价），result4 行数 3050→**3033 行**；结论/摘要同步。
- **新增"参数的第二路线验证"末段（§6.6 内）**：独立物理估计对照
  （ε_c∈[1.4%,28.8%]、ε_p↔31.0%、C_glass 冻结交叉、ρ_sk 文献区间）、
  块 bootstrap 95% CI（4.7/11.9/11.0/3.8/23.9%）、变初值×变 loss ≤0.98%、
  分段拟合幅度差 <2%、四项样本外吻合；**k_p 存疑如实写出**（只有曲线拟合一条路）。
- **嵌入 fig8_药柱3D演化.pdf 与 fig9_截面演化.pdf**（6.3 in 全宽，`width=\linewidth`），
  图注写清读法（锋面内移、半径收缩、表面先干、中心最慢）；§6.4 正文补两图 \ref。
- 编译：xelatex 两遍通过，main.pdf 24→**25 页**，9 图嵌入，0 `??`、0 undefined、0 Warning。
- `check_numbers.py` 断言库的问题 4 旧值全部更新为内生权威值（50.5369/50.5450 序列/
  −0.286/0.074 mm/3033/78.57/60.9%/11.6%/6.64 等），全部通过；`check_refs.py` 通过。

## 1.5 第六轮：评审纠错修订（8 类确定性错误，2026-09-12；依据 `other/修正与力学升级.md` + `03-数据/体积审计.md`）

**只改错、不改已验证数值**（t_f、result、判据、验证层数字一处未动；check_numbers 全过）。

1. **§6.5 单调映射断言（数学错误，最严重）**：删去"任何单一单调的 R(Ū) 映射在数学上
   都不可能复现附件 2"；改为正确数学事实（Ū 单调有反函数，F(u)=R_obs(Ū⁻¹(u)) 且
   F′≥0，**单调映射总能构造出来**），并明确交叉只证明"理想等容基线不是那个映射"；
   "附件 2 更干/更湿"改为"**基线反演的等效含水率更低/更高**"（附件 2 只测半径）。
2. **负孔隙体积（物理错误）**：§6.6 验证段删去"早期 V_pore<0=压密"作守恒证据的写法，
   改为一致非负闭合 πLR²=m_d(1/ρ_s+Ū/ρ_w)+V_g (V_g≥0) + 体积审计结论
   （峰值需求 17.26% @ 2 h、可用 9.94%、缺口 7.33 pp；早期超额 = 孔隙塌陷 ≤9.94% 用尽
   + 基体黏弹压密 ~31% 固体应变；"负孔隙"是伪影；**ρ_sk 只进孔隙诊断、不进 R 预测公式**，
   明确区分预测模型参数与孔隙诊断参数）。
3. **模型正名**："第一性原理收缩闭合"全部降为"**具有物理启发的半经验收缩闭合**"
   （§6.6 开头、§6.5、摘要、fig6/fig7 图注、结论节；g_c,g_p 写明为经验形状函数、
   未从应力平衡/变形/能量推出）。
4. **过强验证结论（两处）**："无过拟合"→写明标定依赖链 R_obs→(Ū,C_s)主解→标定→R_pred
   存在循环、留出只检验同一光滑曲线插值、残差 lag-1 0.82 与 bootstrap 偏斜/多峰，
   **只能说插值稳健**（§6.6 + 附录 D 同步软化）；"独立佐证附件 2"→"**对附件 2 的良好复现**"，
   并注明真正独立验证需完整耦合标定（结论节同步）。
5. **图表版本不一致**：§6.5 写明 −2.4% 属"**均值触发玻璃化冻结**"那条（平台 1.169 cm），
   与图中表面触发（~1.345）/成孔演化（~1.263）不是同一条、参数见数据文件；
   图本身修改列入图表代理清单（见 §7）。
6. **时间口径统一**：删去"约 21 h 后只能平台延拓""半径数据只覆盖过程一小段"
   "35 h 冻结点""比达标早 15.8 h"；统一为"附件 2 覆盖 0–72 h、约 21 h 进入近平台段、
   **t_f 仍在 72 h 观测区间内**"（摘要、陷阱 3、参数表、fig1 图注、§6.4/§6.5、结论节共 7 处）。
7. **附录 A 移动域推导重写**：改为双守恒正确推导（干物质守恒 + 水分守恒、j=−ρ_d D U_r，
   相减得 ρ_d(U_t+vU_r)=(1/r)∂_r(rρ_dDU_r)，再于密度均匀+仿射假设下化材料坐标形式）；
   注明旧写法不成立之处与**固定半径退化测试检验不了此缺口**（v≡0）。
8. **g_c/g_p 机制归属与参数文档**：§6.6 增列四条结构局限（重复归因可能；
   g_p 饱和不自动保证平台；**C_s,min=0.0488 为轨迹提取最小值**（非预设平衡值）；
   毛细压只约束应力量级、塌陷幅度只能标定）。

**check_numbers.py**：断言库同步——删改与错误结论绑定的表述，
新增 14 项"纠错修订"断言（单调映射总能构造出来/半经验/插值稳健/完整耦合标定/伪影/
17.26%/7.33/9.94%/31% 固体应变/C_s,min=0.0488/近平台段/72 h 观测区间/检验不了/
基线反演等效含水率），全部通过；编译 25→27 页、0 错误 0 Warning。

## 1.6 第七轮：全文口径统一（问题 4 主模型 = 内生半经验闭合，附件 2 = 验证数据）

- 图表代理发现的唯一矛盾句（§6.5 开头"主模型仍以附件 2 为外生几何"）已改为统一口径：
  **问题 4 主模型 = 物理启发的半经验收缩闭合内生预测半径（t_f=50.5369 h）；
  附件 2 降为验证数据**（复现 RMSE 0.047/0.074 mm；外生几何版 50.8230 h 作交叉验证 −0.56%）。
- §6.6 结论补"半经验"局限重申与"力学驱动升级是后续正统方向"一句。
- 全文检索"外生几何/仍以附件 2/实测半径"：其余各处（摘要、§6.4、§6.6、结论）本就是
  "附件 2 作交叉验证/降为验证数据"的正确口径，无打架句。
- `check_numbers.py` 新增 3 项口径断言（主模型口径句、降为验证数据、力学驱动升级方向），
  全部通过；编译 27 页、0 错误 0 Warning。

## 1.7 第八轮：力学驱动内生收缩模型（A 级扩展研究）进论文

- **新增小节 §6.7"力学驱动内生收缩模型（扩展研究）"**（§6.6 后、敏感性前），
  五项内容齐全：① A 级力学框架（有限应变非仿射、孔隙非负互补活动集、
  线性检验毛细本构声明、单松弛黏弹骨架、初始平衡、R(t) 是输出）；
  ② 退化检查表（tab：力学退化，7 条全过 + 体积核验式残差 ≤2.7e-16）；
  ③ 自然机制（后期平台自然形成、g 缓增、表面 De 4.7e3→3.3e6，结皮由材料响应产生）；
  ④ 复现程度诚实表述（Π_c=15：RMSE 0.185 cm、R_end 1.393 vs 1.198 cm、
  **瓶颈是本构不是数值**：线性 p_c 吸力饱和+e*∝p* 抵消，平衡 J 限 ~0.48–0.50，
  附件 2 需 0.36，加密复核 RMSE 相同）；
  ⑤ 材料缺口清单（保水曲线/模量松弛谱/初始湿质量/膨压/化学功/轴向）。
  结论明确写"保水与力学实测数据到位前，**不能声称力学模型复现附件 2**"。
- **fig10_力学升级.pdf** 嵌入（6.3 in 全宽），图注写清"随 Π_c 贴近并形成平台、
  但线性检验本构下平台位置偏高——瓶颈是缺真实保水曲线"；§6.7 正文补 \ref。
- 编译 27→**30 页**，10 图嵌入，0 `??`、0 undefined、0 Warning；
  `check_numbers.py` 新增 15 项力学升级断言（修正一次误覆盖旧断言组的编辑事故），全部通过。

## 1.8 第九轮：§6.7 更新到热通量修正后的准确结论（2026-09-13；依据 `03-数据/力学核查.md` + `03-数据/力学升级_重标定.md`）

- **新增"实现层修正"段**：热通量系数 $(kr/r_q)T_q\to(k/r_q^2)T_q$（缺 $R_0^2/2$）+
  表面热阻（内阻 $r_q^2\Delta q/(2k_s)$、外阻 $r_q/h$）bug 的发现与修复；
  修复后与参考求解器同仿射几何下 $\bar U$ 偏差 0.5–1.3%（修复前 1.6–17.3%）；
  原参数替 bug 背锅（同参数 $t_f$ 59.33→49.67 h、RMSE 0.70→0.537 mm）⇒ 全部定量以重标定为准。
- **框架段本构更新**：线性检验本构 $\bar p_c=(p^*/2)(1-S^2)$ 改为幂律
  $p_c=p^*(1-S)/S^\beta$（解析 $\bar p_c$ 与数值积分一致到 5e-16）。
- **新增"全局应力传递"段**（判别实验：湿内部 J=1.000 但 σ_r 全域 −0.047、
  表皮 σ_t=+4.188、表面内移 0.042 cm ⇒ 真全局解，非逐点体积平衡）。
- **复现程度换新口径**：修正后重标定（β 下界扩 0.02、4 起点同一解）RMSE 0.502 mm
  （急 1.135/缓 0.843/台 0.161）、平台 1.1751 vs 1.198 cm、$t_f$=51.1667 h
  （vs 半经验 50.5369/外生 50.82）；留出 0.568/0.572 无过拟合、β 95% CI [0.095,0.657]；
  $t_f$ 三块分解（50.8873/52.5496/49.6667，+1.66/−2.88，总 −1.22 h）。
  旧 0.185 cm/1.393 cm 一组数字明确标注**不再采用**（线性检验本构，已被取代）。
- **新增"结构性上限"段**：$\bar p_c$ 有限上限 $p^*/[(1-\beta)(2-\beta)]$（β=0.15→0.636p*）+
  吸力局部性 ⇒ 单一 β 给不出"急缩快+平台停"（$J_{end}(\beta)$：0.368/0.356/0.336/0.319/0.218，
  平台 J=0.36 需 β≲0.05，拟合 β=0.105 末端 J=0.3345）——结构上限，非调参/实现问题。
- **主线/扩展诚实定位**：§6.7 开头（主线=精度最高可用模型、扩展=严谨骨架讲清机制结构与
  数据缺口）与结论段（"更严谨但当前精度不如半经验，这不是失败"）呼应；
  §6.6 末尾"力学驱动升级是后续正统方向"补 \S\ref{sec:力学升级} 及现状一句。
- **退化表更新**（重标定参数下复跑）：纯弹性 R_end 1.843→1.717 cm、
  τ≪差 5.4e-9→5.3e-15、τ≫ 1.5e-11→4.3e-11、g_min 0.162→0.145。
- **自然机制段诚实化**：两条机制路线（i 黏弹冻结 De 4.7e3→3.3e6 为情景族演示；
  ii 吸力饱和平衡——标定参数落在快松弛角点 Π_τ 触下界、表面 De≈1.0e-3，数据选择路线 ii）。
- **fig10 图注**：标注图内为早期线性检验本构情景族、定量结论以正文幂律标定为准（图未重绘）。
- 编译 30→**31 页**，0 错误；`check_numbers.py` 力学升级断言组换为新数字（36 项）全过；
  `check_refs.py` 通过（sec:力学升级 已无未引用警告，其余 5 个 section label 为既有模式）。
- 排版清理：§6.7 唯一的 "⇒" 字符（lmroman 缺字形）改为文字表述。

## 7. 需图表代理改的图（本侧文字口径已先行统一）

| 图 | 需改内容 |
|---|---|
| fig6（收缩机制分析） | 版本不一致：补"均值触发玻璃化冻结（平台 1.169 cm、−2.4%）"那条曲线与参数，
  或删/改现有曲线使其与正文所述三条（1.345/1.263/1.169）对应关系写清；
  **绿色曲线初值 ~1.90 cm 须修成 2.00 cm**（初值一致性） |
| fig6/fig1（时间口径） | "35 h 冻结""平台延拓"类标注改为"约 21 h 进入近平台段、附件 2 覆盖 0–72 h" |
| fig7（内生贴合） | 图注/图内文字若有"第一性原理"字样改"半经验"；残差图可保留 |

## 2. 数字一致性自查（P3）

- `tables.tex` 由 `gen_tables.py` 从 `03-数据/paper_tables.csv` 自动生成，表 1–6 共 225 格
  **零手工誊数**；另随机抽查 20 格与正文一致。
- `check_numbers.py` 对 **110+ 项关键数字**做字面断言（含新增小节与两条声明的全部数字），与
  `结果总账.csv`、`q23_summary.json`、`q4_summary.json` 逐项比对。
- **首轮自查发现 15 处不一致，全部为自查脚本自身的转义/归一化问题**
  （数学模式 `$…$` 未归一化、`\t` 被当制表符），正文数字本身 0 处错误；
  增补轮发现 2 处（旧 T7 段落的 0.278/0.149 随段落替换被移除）——已把这两个数字
  写回新小节的滞后交叉诊断中；末次运行输出"全部通过"。

## 3. 评委口径（E1–E16）落实情况（≥8 条）

| 条目 | 落实（一句话） |
|---|---|
| E1（按题取值） | 三套物性按问分用，参数表（表 2.1）题目参数与声明假设分两栏，无文献值替换 |
| E2（声明假设） | §1.3 主动写三个陷阱 + §2.3 六条闭合假设前置（湿边界/外推/无潜热/一维/插值/ρ 解释） |
| E4（判据接精度） | §3.1 判据族表每条配实测反验偏差（Le 0.303 h、Bi_m −46.03 h、Bi_h 0.075 h） |
| E5（不写普适废话） | 全部论述落在本题区间与数字上（如"渗透 3 mm ≪ R=2 cm"），无"干燥就是扩散"式陈述 |
| E6（一致性≠可靠性） | §5 明确 V4/V6 只证实现与离散收敛，不充当可靠性证据 |
| E7（两种不同方法互证） | V1 谱解+Duhamel vs 有限体积（5.9e-7 ℃）、V2 首模解 vs 全耦合尾段，写明能/不能排除 |
| E8（前后自洽） | 四问一张开关表；t₃/t₄ 大小关系给机制归因三件套（×2.26 vs −60.6% → 净 −11.1%） |
| E9（主动声明陷阱） | §1.3 三个陷阱（物性分用/开尔文/附件截断）全部前置声明 |
| E11（不确定度） | §7 七源误差预算，确定性合成 ±1.71 h，分可收敛/固有/情景三类；C_e 单列不进区间 |
| E12（决策量带判据） | t_f 给穿越区间+右端点+严格下界闸门（15.70/19.02 h）+图 4 排除线 |
| E13/E14（算法与复现） | §4 离散/迭代/容差全交代；`run_all.py` 单命令、两次运行 md5 一致（四组哈希写入正文） |
| E15（退化极限） | V5a 移动域退回固定域（Δ=0）、V5b 退回 Bessel 首模（商 0.99996） |

## 4. 结构与内容核对（P2/P4/P6）

- 八节齐全（问题重述/模型/判据/算法/验证/结果/误差/结论）+ 摘要与 6 个关键词 +
  4 条真实参考文献（Luikov 1966、Crank 1975、药典 2020 通则 0212、Saravacos 1986）+
  三个附录（移动域推导、复现清单与代码结构、量纲检查表）。
- 五张必备评分表齐全：参数取值表（分栏）、判据族表（反验列）、验证汇总表（能/不能排除列）、
  敏感性表（全 Δt_f）、误差预算表（≥6 源 + 合成）。
- 全文无占位符、无"待补/暂定/TODO"（grep 自查）；`\includefig` 的备用占位分支在图就位后
  不会被编译进 PDF（已确认 5 图全部嵌入）。

## 5. 遇到的困难与处理

1. **heredoc 吞反斜杠**：bash heredoc 追加 LaTeX 时表格行尾 `\\` 被吞成 `\`，
   导致 `Misplaced \noalign` 编译错误；写 `fix_backslash.py` 逐行修复（67 行）后通过。
2. **图并行生成**：初稿编译时 fig1–5 未就位（`\IfFileExists` 备用分支），并行代理交付后
   重新编译全部嵌入；增补轮 fig6 同样在收尾前就位（6 图全部嵌入，19 页）。
3. **数字自查的假阳性**：首轮 15 处"不一致"全是脚本匹配问题（见 §2），
   通过 `$` 归一化 + 字面匹配修正；增补轮 2 处为旧段落替换带来的真实缺口（已补写）。
4. **表 6 留空语义**：`gen_tables.py` 对 nan 格输出"—"并在表注说明"超出表面留空不外推"，
   与 result4 口径一致。
5. **口径统一**：旧 T7 段落（35 h 冻结、反推失水对比）与新内生分析小节部分重叠，
   旧段落替换为新小节的引导句；全文"冻结时刻"口径统一为"约 21 h 起平台"（4 处）。

## 6. 复现入口

```
cd A/06-论文
python gen_tables.py        # 由 paper_tables.csv 生成 tables.tex
xelatex main.tex && xelatex main.tex
python check_numbers.py     # 数字一致性自查（应输出"全部通过"）
```
