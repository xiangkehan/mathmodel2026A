# fig1–fig10 出版级样式规范与重构说明

日期：2026-09-12。范围：**只改样式，不改内容与数据**——曲线、标注信息、数字口径与原版一致
（数字仍全部来自 `03-数据/`，脚本内不誊写结果）。脚本：`02-代码/plot_all.py`（fig7–fig10 陆续加入，同规范；fig8/fig9 为 3D 曲面与场图见 §十，fig10 为力学情景三联图见 §十三）。
参考规范：`other/参考/math-modeling-skill-main/tools/figure/SKILL.md`（按最终尺寸出图 / 矢量优先 /
色盲友好 + 冗余编码 / 字号 7–9 pt）与 `.../references/quality/publication_checklist.md`（图例无框、
面板标签、柱状从零、无 rainbow/jet、中文缺字必查）。

## 一、样式基线（`plot_all.py` 的 `RC` + `style_axis()`/`panel()`）

| 项 | 取值 |
|---|---|
| 后端 / 导出 | matplotlib Agg；`savefig.dpi=300`、`savefig.bbox=None`（按 figsize 精确出图，**导出后不再缩放**） |
| 字体 | Microsoft YaHei（`pdf.fonttype=42` 嵌入 TrueType 子集）；`axes.unicode_minus=False` |
| 字号 | `font.size` 8、轴标签 8、刻度 7.5、图例 7.5、标题 8.5、面板标签 9 加粗；**全部 ≥7.5 pt** |
| 线宽 | 曲线 1.2–1.6、参考线 1.0–1.2、脊柱 0.7 |
| 脊柱 / 网格 | 白底、去顶/右脊柱（双 y 轴面板保留右脊柱承载右轴）；网格默认关闭，线图仅开**极浅** y/x 网格（`0.86`, lw 0.4） |
| 图例 | 无框（`legend.frameon=False`），项数 ≤5（见 §七 例外） |
| 面板标签 | `(a)/(b)` 粗体小写，置于各面板左上外侧（`transAxes`） |
| 标题 | ≤28 字符；**图内不放说明性长文字**（见 §九：论述全部由论文 `\caption` 承载） |
| 禁用 | 3D 轴、彩色渐变叠加、装饰纹理、jet/rainbow（时间族统一 viridis，感知均匀、灰度单调） |

## 二、色板与语义映射（Okabe-Ito，色盲安全）

色板（`OI`）：蓝 `#0072B2`、橙 `#E69F00`、绿 `#009E73`、红橙 `#D55E00`、品红 `#CC79A7`、
天蓝 `#56B4E9`、中性灰 `#6B7280`、深灰 `#222222`。

语义映射（`ROLE`，**同一变量跨图同色**）：

| 语义 | 色值 | 出现在 |
|---|---|---|
| 实测数据 / 附件 | 深灰 `#222222` | fig1(b) 附件2 R(t)、fig6(a)(b) 附件2 实测与等效含水率 |
| 模型主解 | 蓝 `#0072B2` | fig4 问题4 主解、fig6 闭合3'（最接近的零拟合闭合）、fig6(b) 模型平均含水率、fig5(b) “可收敛” |
| 模型次解 | 天蓝 `#56B4E9` | fig4 问题3 主解 |
| 对照 / 参考解 / 基线 | 红橙 `#D55E00` | fig4 对照（附4+固定 R₀）、fig6 闭合1 全局理想基线、fig5(a) 偏晚、fig5(b) “情景假设” |
| 解析解 | 绿 `#009E73` | 保留于 `ROLE["analytic"]`（本批图用**线型/标记**区分解析与数值，见下） |
| 次要物理量：温度 T | 橙 `#E69F00` | fig1(a) T_a、fig5(b) “口径依赖” |
| 次要物理量 / 第三机制 | 品红 `#CC79A7` | fig1(a) C_env、fig6 闭合5 成孔演化、fig5(b) “固有” |
| 阈值 / 严格下界 / 平台参考线 | 中性灰 `#6B7280`（一律虚线/点线） | fig3(b) 0.15 阈值、fig4 阈值与下界、fig6 平台线 |
| 时间族（有序连续量） | viridis 连续色 | fig2 三个时刻、fig3 两个剖面面板的各 6 个时刻 |
| fig5(b) “不含 C_e 口径”合成行 | 中性灰 `#6B7280` | fig5(b) |

冗余编码（灰度可辨的保证）：fig2 用**空心标记形状**（o/s/^）区分三个时刻、实线=解析/点=数值；
fig3 用 viridis 的**明度单调**；fig4 三条解用 实线/实线(粗)/虚线；fig6 四条曲线用 实线(粗)/虚线/实线/点划线；
fig7 三条曲线用 实线(粗)/虚线/点线（实测 / 内生模型 / 失水基线）。

## 三、逐图设计宽度与字号（论文真实包含尺寸）

| 图 | 设计尺寸 (in) | 纵横比 | 论文包含宽度 | 最终字号 |
|---|---|---|---|---|
| fig1_输入数据 | 6.3 × 3.4 | 0.54 | **`width=\linewidth`** | 标题 8.5 / 正文与轴标 8 / 刻度·图例 7.5 |
| fig2_问题1解析对拍 | 5.4 × 3.2 | 0.59 | `0.86\linewidth`（=5.4 in） | 同上（插图内亦 ≥7.5） |
| fig3_温湿剖面 | 6.3 × 3.4 | 0.54 | **`width=\linewidth`** | 同上 |
| fig4_达标穿越 | 5.4 × 3.2 | 0.59 | `0.86\linewidth` | 同上 |
| fig5_敏感性 | 6.3 × 4.2 | 0.66 | **`width=\linewidth`** | 同上 |
| fig6_收缩机制分析 | 6.3 × 3.8 | 0.60 | **`width=\linewidth`** | 同上 |
| fig7_内生收缩贴合 | 6.3 × 3.8 | 0.60 | **`width=\linewidth`** | 同上 |
| fig8_药柱3D演化 | 6.3 × 3.8 | 0.60 | **`width=\linewidth`** | 同上 |
| fig9_截面演化 | 6.3 × 3.9 | 0.62 | **`width=\linewidth`** | 同上 |
| fig10_力学升级 | 6.3 × 3.4 | 0.54 | **`width=\linewidth`** | 同上 |

PDF 页面尺寸实测等于 `figsize`（`/MediaBox` 校验：6.300×3.400、5.400×3.200、6.300×3.400、
5.400×3.200、6.300×4.150、6.300×3.800、6.300×3.800、6.300×3.800、6.300×3.900、6.300×3.400 in），故**图中的 7.5–8.5 pt 就是论文里的
最终字号**。凡标 **`\linewidth`** 的八张（fig1/fig3/fig5/fig6/fig7/fig8/fig9/fig10，均因双子图/三联网格、
全宽数据区或 3D/场图面板无法压到 5.4 in）需论文侧把 `\includegraphics[width=0.86\linewidth]` 改为 `width=\linewidth`；
fig2/fig4 保持原样。

## 四、审计（`audit_figure()`，`python plot_all.py` 自动逐张执行并汇总）

审计项：最小字号、标题长度、图例项数、刻度标签两两重叠、柱状基线是否在 0、colorbar 数、
3D 面板数、PDF 内嵌位图数、>25 点密集标记未用 `markevery`、**文字裁切**、**图注压住 x 轴标签**、
纵横比；另有 logging + warnings 双通道缺字检查。全部 PASS 才退出码 0。

```
图 | 尺寸(in) | 纵横比 | 最小字号 | 最长标题 | 图例项 | 刻度重叠 | 柱基线 | 裁切 | colorbar | 3D | 嵌入位图 | 密集标记 | 结论
fig1_输入数据     6.3×3.4 | 0.54 | 7.5 | 11 | [2]     | 0 | 0 | 0 | 0 | 0 | 0 | 0 | PASS
fig2_问题1解析对拍 5.4×3.2 | 0.59 | 7.5 | 16 | [3]     | 0 | 0 | 0 | 0 | 0 | 0 | 0 | PASS
fig3_温湿剖面     6.3×3.4 | 0.54 | 7.5 | 14 | [6,6]   | 0 | 0 | 0 | 0 | 0 | 0 | 0 | PASS
fig4_达标穿越     5.4×3.2 | 0.59 | 7.5 | 15 | [4]     | 0 | 0 | 0 | 0 | 0 | 0 | 0 | PASS
fig5_敏感性      6.3×4.2 | 0.66 | 7.5 | 20 | [5]     | 0 | 0 | 0 | 0 | 0 | 0 | 0 | PASS
fig6_收缩机制分析  6.3×3.8 | 0.60 | 7.5 | 16 | [4,2]   | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | PASS
fig7_内生收缩贴合   6.3×3.8 | 0.60 | 7.5 | 14 | [3]     | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | PASS
fig8_药柱3D演化    6.3×3.8 | 0.60 | 7.5 | 18 | []      | 0 | 0 | 0 | 0 | 0 | 2 | 2 | 0 | PASS
fig9_截面演化      6.3×3.9 | 0.62 | 7.5 | 20 | []      | 0 | 0 | 0 | 0 | 1 | 0 | 7 | 0 | PASS
fig10_力学升级     6.3×3.4 | 0.54 | 7.5 | 17 | [3]     | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | PASS
[字体] 无缺字告警（logging + warnings 双通道）；[门禁] PASS：10 张图审计全部通过
```

- 缺字：0（中文无乱码/豆腐块，已按 300 dpi 全分辨率逐张目视复核）。
- 刻度重叠：0；柱基线：fig5 两面板 15 根柱全部自 0 起（含负向 −0.0065 h 的发散柱）。
- 无 colorbar、无 3D、PDF 内无位图（纯矢量，`/Subtype /Image` 计数为 0）。
- 裁切 0、图注压轴标 0：§九 清理掉图底文字后，`cap_hit` 检查（`fig.text` 与各面板 x 轴标签重叠）
  成为常备护栏；此后若再加图底文字，会被它拦下。

## 五、灰度预览结论（`05-图表/_qa/*_gray.png`，由 PNG 转 L 模式生成）

| 图 | 灰度可辨性 |
|---|---|
| fig1 | (a) 温度实线 vs 水分虚线，线型可辨；(b) 单曲线 + 虚线延拓 |
| fig2 | 三个时刻用 o/s/^ 空心标记 + viridis 明度差（0.15/0.53/0.77），均可辨 |
| fig3 | 6 时刻曲线靠 viridis 明度单调（早浅晚深）区分，图例给出对应关系 |
| fig4 | 问题3（浅实线）/问题4（深实线）/对照（虚线）三者在灰度下可辨 |
| fig5 | 柱条由**各自 y 轴类别名唯一标识**，颜色图例为冗余信息，灰度下不丢信息 |
| fig6 | 附件2（粗实线）/结皮（实线）/理想（虚线）/成孔（点划线）线型+明度双重可辨 |
| fig7 | 实测（粗实线）/内生模型（虚线）/失水基线（点线）线型+明度三重可辨；残差带为单曲线+零线+包络虚线 |
| fig8 | viridis/magma 在灰度下明度单调：曲面高度+底面 $R(t)$ 轨迹仍可读出锋面内移与收缩 |
| fig9 | viridis 灰度单调：$t$=5/15 h 圆盘的“中心亮=湿、边缘暗=干”径向梯度仍清晰 |
| fig10 | (a) 实测粗实线 / 模型虚线 / 基线点线 三重可辨；(b)(c) 单曲线 + 参考线，灰度下不丢信息 |

## 六、例外与已知限制（均已在上表审计中判定为通过）

1. **fig5 纵横比 0.66 > 0.62 上限**：该图为“长中文类别名”的两行竖排（用户规范允许的排法）。
   实测压到 0.62 时 7.5 pt 类别名会互相压盖（正是本审计的硬门槛之一），故放宽到 0.70 上限
   （`ASPECT_EXEMPT`）。其余 5 张均在 0.50–0.62 区间。
2. **fig3 图例每面板 6 项 > 5 项**：颜色编码的是**有序连续时间族**（非 6 个并列类别）。该族末段曲线
   在 r→2 处重合（24/36/57 h 的 C 相差 <0.011 kg/kg），改直接标注会互相压盖，故保留 6 项
   ncol=2 图例（`LEG_MAX_EXEMPT`）。
3. **fig1(a) 保留右脊柱**：双 y 轴必需（左轴温度、右轴水分），非“多余脊柱”。

## 七、上一轮问题的修复

- **fig5 的 `⇒`(U+21D2) 缺字**：上一轮该告警走 `warnings` 通道、脚本只拦了 `logging`，故漏检并留下豆腐块。
  本轮 `save()` 用 `warnings.catch_warnings(record=True)` 捕获 `missing from font` / `Glyph` 告警并计入
  门禁（缺失即 `exit(1)`）；已用 ⇒ / → 两个字符实测验证：⇒ 被捕获（1 条），→（U+2192，YaHei 有）
  无告警。fig5 中的箭头改为 `→`，因此在通过审计的同时不再有豆腐块。
- 顺带新增两条形式检查（本轮实际各抓到过真缺陷）：**文字裁切**（`bbox=None` 下超出画布会被静默切掉）
  与**图注压住 x 轴标签**。

## 八、复现

```bash
cd 02-代码 && PYTHONIOENCODING=utf-8 python plot_all.py        # 出全部 6 张：PDF + 300dpi PNG + _qa 灰度预览 + 审计
PYTHONIOENCODING=utf-8 python plot_all.py fig3                 # 只出某一张（调试）
PYTHONIOENCODING=utf-8 python plot_all.py --no-cache           # fig4 对照曲线强制重算（约 1–2 min）
```
退出码 0 = 6 张图审计全过且无缺字；非 0 = 有 FAIL（并打印不合格项）。

## 九、图内长文字清理（2026-09-12 收尾）

**动机**：上一版把数据源/口径/偏差数字等论述用 `caption()` 写成图底多行 `fig.text` 烤进 PDF——
它会随图缩放、与 LaTeX `\caption` 重复，违反“完整论述移到图注”的规范。

**处置**：删除 `caption()` 辅助函数与 6 张图的全部调用，图内**只保留**：面板 `(a)/(b)` 粗体标签、
≤28 字符短标题、坐标区内的箭头/三段标记/交叉点圈/阈值线与下界线/短文字（如“平台 1.198”“早期塌陷
峰值超额 0.182 cm”）、图例（含图例下的分类图例块，属图例不属论述）。删后各图 `bottom` 由 0.29–0.35
收到 0.155–0.205（fig5 保留图例空间 → 0.205），坐标区因此变高，`figsize` **不变**，故
§三 的设计宽度与 §四 的字号结论完全保持（最小字号仍 7.5 pt）。PDF 体积下降 10–25 kB/张。
（fig7 是此后新建的图，从未有过图底文字块，`bottom=0.155`。）

**关键信息未丢**：原本只出现在图底文字里的数字，现由两条路径承载——
（1）论文 `\caption`（下表即移交清单）；（2）`plot_all.py` 每次运行的 `[F1]…[F7]` stdout 诊断行
（已补齐这些数字，运行日志即可溯源）。

### 移交论文侧补入 `\caption` 的要点（图内没有、数据源已标）

| 图 | 原来只在图底文字里的要点 |
|---|---|
| fig1 | 附件1 仅给到 4 h（14400 s），其后按标签末值延拓（主口径/声明假设）；35–72 h 半径仅降 **0.002 cm**；72 h 后 1.198 cm 平台延拓供问题4 使用 |
| fig2 | 解析 vs 数值逐点最大偏差 **3.13e-06 ℃**（源 v1_points_final.csv），远低于四位小数分辨率 5e-05 ℃；偏差随 N 单调下降且与 N^-2 平行，拟合阶 **2.000/1.993/1.990**（阶数在插图图例中已有） |
| fig3 | 温度：Le=**10.69–640.6**，t=3 h 时全径仅 **49.8–50.2 ℃**；水分：**t_f=57.18 h** 时中心 0.1502 仍高于表面 0.0526，36→57 h 中心仅由 0.1854 降到 0.1502（差 **0.0352**）；左端圆点=中心值 |
| fig4 | 效应拆分：① 附4+固定 $R_0$ = **129.10 h**；② 附4+内生 $R(t)$ = **50.54 h**；③ 问题3（附3+固定 $R$） = **57.18 h**；收缩效应 ①−② = **78.57 h（60.9%）**、净效应 ③−② = **6.64 h（11.6%）**；严格下界余量 **41.48 / 31.52 h**（源 v3_bound.csv、q4_bound.csv） |
| fig5 | **基准解 Δt_f=0 的三行未画柱**（C_e=0.04986 主口径；半径插值 PCHIP；环境外推:末值保持）；红=偏晚、蓝=偏早；C_e 口径单因素 +22.63 h 是其余敏感性因素之和（**1.92 h**）的 **12 倍**；误差预算源 error_budget.csv、共 8 条、横轴对数、颜色=类型（灰=不含 C_e 口径的合成行） |
| fig6 | C_glass=**0.21**（均值触发）与骨架密度 **1468**；**四条零拟合闭合的平台值与偏差**：闭合1 全局理想 1.123（−6.3%）、闭合4b 表面触发结皮 1.345（+12%）、闭合3' 均值触发结皮 **1.169（−2.4%）**、闭合5 成孔演化 1.262（+5.3%）；均值触发闭合的冻结时刻 23.5 h（相对附件2 口径分界 21 h 为 +12%）；早期应变 **9.1%**（峰值超额 0.182 cm）；缺口 **0.129→0.155 kg/kg**（@35→72 h）；“两侧符号相反 ⇒ 单一单调 R(Ū) 映射不可能复现”这一结论句；三相（A/B/C）按“约 21 h 进入近平台段”表述 |
| fig7 | 模型结构（失水 × 毛细-膨压塌陷 × 结皮-成孔停滞，一项一机制）与 **5 个标定参数**（ε_c=0.1108、a=1.668、b=1.220、ε_p=0.0709、k_p=3.787；$C_{glass}$=0.21 为文献值非拟合）；**RMSE=0.0473 mm**（附件2 量化分辨率 0.01 mm 的 4.7 倍）；残差 **min −0.122 mm @3.0 h**、包络 **±0.162 mm**；B 段（3.5–21 h）|残差|max **0.124 mm**；平台 **1.1969 vs 1.198 cm（−0.09%）**；滞后交叉 **预测 16.8 h vs 实测 16.0 h**；残差 lag-1 自相关 0.82（非白噪，系统形状）；数据源 endogenous_calibrated.csv、模型说明 03-数据/内生收缩模型.md |

fig6 的三段标记（A/B/C、占 61%）、平台 1.198/1.169 cm、峰值超额 0.182 cm、交叉 16.0 h、
“附件2 更干/更湿”等**仍在图内**，无需重复写进图注。

## 十、3D 曲面与场图（fig8/fig9，2026-09-12 增补）

**数据源**：`03-数据/q4_endo_fields.npz`（内生问题4 的完整时空场，见同目录 README）：固定物理网格
`r_cm` 0–2.0 cm 步 0.02、帧间隔 360 s（507 帧，0→t_f=181932.7 s=50.54 h）、`C`/`T` 域外（r>R(t)）为 NaN、
`R_t` 为闭合 C 预测的外半径（2.0000→1.1899 cm，单调不增）。

- **fig8(a) 水分场曲面**：x=$r$、y=$t$、z=颜色=$C$（viridis），域外 NaN 不画 ⇒ 右边界即 $R(t)$；
  底面另画 **$R(t)$ 轨迹**（深灰线 + 行内 “$R(t)$” 标注），把“半径随时间收缩”变成可直接读出的曲线。
  视角 `elev=22, azim=-122`，可同时看到锋面（曲面上自表及里的陡降）随时间内移与域界内缩。
- **fig8(b) 温度场曲面**：同布局，颜色/高度=$T$（magma，感知均匀）；热场准稳态表现为近似水平面。
- **fig9 截面演化**：6 个代表时刻（0/5/15/25/40/50 h）把 $C(r,t)$ 插值到 Cartesian 圆盘网格，
  $r>R(t)$ 留白（`set_bad("white")`），6 盘**共享同一色标**（全域 min–max 0.0589–2.5500 kg/kg）与
  一个 colorbar；每盘虚线圆为初始半径 2.0 cm ⇒ 收缩一目了然。
- 两图均为 6.3 in 全宽、字号 ≥7.5 pt、面板标签 `(a)/(b)` 粗体、无图内论述文字块。

**审计规则随之细化**（fig1–fig7 的判定不变）：
1. colorbar 不再要求数量为 0，改为**每根 colorbar 必须有 label（含单位）**；横/竖 colorbar 均可
   （label 读 `ylabel` 或 `xlabel`）。fig8 两根（C、T 各自量纲）、fig9 一根（6 盘共享）。
2. 嵌入位图上限改为 `≤ 场图面板数 + colorbar 数`：imshow 场图与 colorbar 色带本质是栅格；
   线/柱/散点面板若被栅格化仍判 FAIL（fig1–fig7 的图片计数仍为 0）。
3. **3D 面板的刻度/轴内文字的 window extent 经投影后不可用**，程序无法判其裁切（实测可差 4 个数量级）；
   审计对 3D 文字跳过裁切判定并**打印提示**，其位置只能目视核对——fig8 已逐块目视确认无裁切/重叠
   （含 z 刻度、轴标、colorbar 与面板标签四处边界）。

**需论文侧补入 `\caption` 的要点（图内没有）**：
- fig8：数据源与口径（内生问题4 时空场、固定物理网格、帧间隔 360 s、域外 NaN）；$C$ 全域
  0.0589–2.5500 kg/kg、$T$ 全域 28.00–50.17 ℃；$R(t)$ 2.0000→1.1899 cm；t_f=50.54 h；
  底面深灰线即 $R(t)$。
- fig9：6 个时刻的选取（0/5/15/25/40/50 h）；每盘标注的 $R(t)$ 与色标范围（=全域 min–max，
  故末期对比被压缩，为口径要求）；虚线圆=初始半径 2.0 cm；抽查一致性
  （t=25 h：C(0)=0.2701、C(0.5)=0.2479、C(1.0)=0.1716，与 npz 逐点相同）。

## 十一、问题4 口径切换：fig4 改用内生几何（2026-09-12）

**背景**：`03-数据/结果总账.csv` 把“问题4 主值”改为**内生几何**版（50.536869 h，
`q4_endo_check.csv` 的 `tf_endo_h=50.53686860`，与 `q4_endo_fields.npz` 末帧 t_f=181932.7 s 一致）；
此前 fig4 画的是附件2 外生几何解（50.8230 h，`q4_main_steps.npz`），与主结果不符。

**改动（只动 `02-代码/plot_all.py`）**：
- fig4 的“问题4 主解”曲线改为内生版：`max_q U(q,t)` = `q4_endo_fields.npz` 的 `nanmax(C, axis=1)`
  （逐帧对 r≤R(t) 的有效单元取最大，域外 NaN 自动排除），临值穿越 t_f=**50.5362 h**（线性插值）/
  50.5369 h（末帧步末口径）；图例写“问题4 主解（内生几何 + $R_{pred}(t)$）：$t_f$=50.54 h”。
- 恢复硬断言：`|图上 t_f4 − 总账内生主值| < 0.01 h`（插值穿越与步末值差 0.0007 h），否则判失败。
- 效应拆分改为 ①②③ 结构（见 §九 fig4 行）：收缩效应 ①−② = 78.57 h（60.9%），净效应③−② = 6.64 h（11.6%）。
- 其余元素不变：问题3 主解（57.18 h）、对照附4+固定 $R_0$（129.10 h，读缓存）、阈值线、
  两条严格下界竖线、穿越放大插图。

**其余图的核对结果**：
- **fig8/fig9**：本就使用 `q4_endo_fields.npz`（内生几何），无需改动。
- **fig6**：右面板的 $ar U_{model}(t)$ 与各闭合曲线的驱动场来自**附件2 几何（外生 R）**的求解
  （源 `endogenous_shrinkage.csv`，由 08-临时/endogenous_analysis.py + q4_main_steps.npz 生成，
  两者都不在本任务可写范围）。**未切换数据**，只把图例里的“（问题4）”改为“（附件2 几何求解）”，
  避免与新的主口径混淆。原因（实测）：若改用内生驱动（`nanmean(C)`），
  $ar U_{inf}-ar U$ 在 0.5 h 由 −0.231 变为 **−0.334**、滞后交叉由 16.0 h 移到 **≈20 h**，
  与 `03-数据/内生收缩分析.md`（本图数字的唯一出处，非本任务可写）的记载冲突。
  如需彻底切到内生驱动，须先重跑 08-临时/endogenous_analysis.py 并更新该 md 与 CSV。
- **fig7**：`R_pred` 是**标定后的收缩闭合**（以内生收缩模型命名，非“问题4 几何”），
  其驱动场同样是附件2 几何，与 `03-数据/内生收缩模型.md` 的 0.0473 mm 口径一致，无需改动。
  一致性交叉核对（供参考）：新主解耦合 $R_t$ vs 标定 $R_{pred}$ 的 max|差|=0.071 mm、RMSE=0.057 mm；
  耦合 $R_t$ vs 附件2 实测 RMSE=**0.074 mm**（max 0.167 mm）——耦合后略松于单机标定的 0.047 mm，
  属几何-湿度场回耦的正常代价，已记录不修改图。

## 十二、与论文文本口径对齐的一轮修改（2026-09-12）

论文侧已完成一轮纠错（模型正名为**物理启发的半经验收缩闭合**；时间口径统一为“**约 21 h 进入近平台段**、
$t_f$ 在 72 h 观测窗内”；平台偏差 −2.4% 明确对应**均值触发**玻璃化冻结闭合、平台 1.169 cm），
据此同步两张图与两处措辞：

**fig6_收缩机制分析**（6.3×3.8 in，未改尺寸）
- **补入第 4 条闭合**：左图新增 `R4b_crust_collapse_cm`（理想+表面触发结皮+毛细塌陷，绿色），
  使论文图注所述“表面触发（平台约 1.345 cm）/ 成孔演化（约 1.263 cm）/ 均值触发（1.169 cm，−2.4%）”
  三条闭合与“全局理想”基线同时在场；图例改为 5 项（附件2 实测 + 四条闭合），标题改为“半径对比：四条零拟合闭合”。
- **平台值与偏差直标**在各自平台一端：1.345（+12%）、1.262（+5.3%）、1.169（−2.4%），
  加上附件2 的“平台 1.198 cm”——与 `03-数据/内生收缩分析.md` §2 表的 +12% / +5.3% / −2.4% 一一对应。
- **绿线初值修正**：`R4b` 列在 t=0 为 1.900 cm（分析脚本“常数 5% 塌陷”起值），与初始条件不符，
  已按初始条件置为 **R(0)=2.000 cm**（仅首点；0.5 h 起的值仍为该列原口径，如 1.854 cm）。
- **时间口径**：三段标记 C 段由“冻结”改为“**C 近平台段**”；[F6] 诊断行同步为“≈21 h 起近平台”。
- 右面板 $ar U_{model}$ 图例保留“（附件2 几何求解）”限定（其驱动场口径，见 §十一）。

**fig7_内生收缩贴合**（未改数据）：图例中模型名由“内生模型”改为论文正名
“**物理启发半经验闭合** $R_{pred}(t)$（RMSE=0.047 mm）”。全文（含脚本）**无“第一性原理”字样**，
无需替换。

**fig1_输入数据**（顺带统一口径）：(b) 面板标注由“$t$=35 h：$R$ 首达 1.200 cm 此后基本冻结”
改为“**≈21 h 起进入近平台段**（$t$=35 h 首达 1.200 cm）”，竖线/底纹移到 21 h；标题改为
“附件2：半径收缩与近平台”；[F1] 诊断行同步。35 h 仍是数据事实（三位小数下首达 1.200 cm），保留。
**fig7_内生收缩贴合**：(a) 面板 A/B/C 三段标记同口径改为“**C 近平台段**”。
（fig8 的两个 3D 面板本就未画三段标记，无此措辞；fig9 面板标题只含 $t$ 与 $R$，均无需改动。）

**[F6] 诊断行现在报告四条闭合的平台与偏差**（含表面触发 1.3449 cm / +12.3%），供论文图注核对。

## 十三、fig10 力学升级：修正后重标定的幂律力学模型（2026-09-12 换版）

**换版原因**：此前 fig10 画的是**线性检验本构的情景族**（Π_c=0.2/1/5 扫描，RMSE 0.30–0.72 cm），
该内容已被**修正后重标定**取代。现 fig10 与论文“幂律吸力力学模型标定”一节一致。

**数据源**：`03-数据/mechRefit.csv`（145 点 `R_data_cm`/`R_pred_cm`/`residual_mm`）+
`mechRefit_fields.npz`（重标定参数 `fit_params=[p*, β, Π_τ, τ_exp]`、全程 `Um`/`DeS`/`gS`、
末端平衡表 `terminal`）；口径见 `03-数据/力学升级_修复重标定.md`
（A04/A05 修复后重标定，取代 `力学升级_重标定.md` 的全部数值）。**不再使用** `mechA_*` 旧情景数据，
也不再在图内现算任何情景（求解器不参与本图，全部数字来自上述两文件）。

**三联内容**：
- **(a) 重标定拟合**：附件2 实测（深灰粗线）+ 重标定力学模型 $R_{pred}$（蓝虚线，图例给
  **RMSE=0.522 mm**）+ 失水理想基线（红橙点线，由同一求解的 $ar U(t)$ 等容反推
  $R_0\sqrt{(1+ar U)/(1+C_0)}$，非另取口径）；标三段（急缩/缓缩/平台）与两侧平台值
  （**实测 1.198 / 模型 1.1747 cm**）。
- **(b) 残差**：$R_{pred}-R_{data}$（mm）与零线，0–21 h 底纹标“系统性欠缩（峰值 +1.59 mm
  @ 2.0 h，峰值与箭头位置由数据现算）”，
  平台段标“贴合”，并给出**分段 RMSE 1.193/0.875/0.163 mm**——如实呈现“单 β 吸力给不出
  急缩快 + 平台停”的结构性残差。
- **(c) 平台机制**：归一化平均吸力 $ar p_c/p^*$ 随饱和度 $S$ 的曲线，**$S	o0$ 时饱和于有限上限
  $1/[(1-eta)(2-eta)]p^*$**（本标定 β=0.1059 ⇒ **0.591 $p^*$**，图中虚线），标注
  “⇒ 末端 $J$ 有限 → 平台”；并明示 **$Depprox1.0	imes10^{-3}\ll1$：非结皮冻结**——
  平台来自吸力饱和平衡，不是 Deborah 数驱动的结皮冻结。

**现算/读取值与文档核对（脚本内断言 + stdout）**：RMSE **0.5220 mm**（文档 0.522）✓；
分段 **1.193 / 0.875 / 0.163 mm** ✓；平台 **1.1747 vs 1.198 cm** ✓；max|残差| **1.593 mm @ 2.0 h** ✓；
$De$ **1.00e-3**（≪1）✓；$g_{min}$ **0.1446＞0** ✓；β=0.1059、$p^*$=1e4 ✓；
吸力上限 0.5905 $p^*$ ✓；末端平衡表（npz `terminal`）内插 β=0.1059 → $J_{end}$ **0.3343**
（$R_{end}\approx1.156$ cm）✓。$t_f=51.1667$ h 与 $J_{end}$ 均读自 `mechRefit_fields.npz`
（`t_f_h`、`terminal`），[F10] 诊断行复述。

**需论文侧补入 `\caption` 的要点（图内没有）**：标定参数（$p^*$、β=0.1059、$\Pi_	au$、
$	au_{exp}$ 与各自触界情况）；$t_f=51.17$ h 与半经验 50.54 h / 附件2 外生 50.82 h 的对照；
max|残差| 1.59 mm 与 lag-1 自相关 0.98（残差为急缩段系统性滞后）；结构性张力
（平台可达需 β≲0.02–0.05，而中段够快需 β≳0.10，折中 β≈0.106 的末端
$J_{end}=0.3343$ 低于附件 2 的 0.3588（半径欠 0.42 mm））；
7 条退化检查与留出/bootstrap 结论。

## 十四、图与已修正数据/文本的同步（2026-09-12 第四波 a）

**背景**：第三波把数据与论文文本改到「内生几何主解 + 修复后力学重标定 + q4_endo_eval 满 72 h 口径」，
但 `05-图表/` 的图仍是旧数据生成的；本波只动 `02-代码/plot_all.py` 与图，不改任何数据文件。

**fig5_敏感性（6.3×4.35 in，data 与版式都改）**
- **问题4 效应拆分三根条改读 `q4_split.csv`**（内生几何主解为基准）：
  `附录4固定R(对主解)` **+78.2752 → +78.5678 h**、`问题3(对主解)` **+6.3504 → +6.6431 h**、
  第三根由旧外生主解行（−0.0065，基准是外生 PCHIP 50.8295 h）
  改为 `附录4+外生R(t)(交叉验证)` **+0.2862 h**（= 50.823025 − 50.536869，正式进图）。
  `sensitivity.csv` 的三个旧外生拆分行（`问题4效应拆分:*`）**不再进图**；
  组间注记由“问题4 效应拆分（T6 归因）”改为“问题4 效应拆分（基准：内生主解）”，
  与论文表 13 的“（对主解）”一致。`read_q4_split()` 用 t_f 现算派生行并与 csv 自带的
  `shrink_effect_endo_h`/`net_effect_endo_h` 断言核对（1e-6）。
- **其余敏感性条数据源未变**（仍读 `sensitivity.csv`）：C_e=0.1366 **+22.6261 h**、
  环境外推线性 **+1.6194** / 末 1 h 均值 **+0.3025**、半径插值线性 **+0.0022**（逐条与 stdout 核对）。
- **(b) 误差预算适配 `error_budget.csv` 的新表**（第三波把 `类型` 列改为 `类别/适用` 且 8→11 行，
  旧代码按键 `类型` 取色会 **KeyError**）：类别改按**误差源前缀**判定，
  与论文表 14 的三类分报一一对应——
  `数值误差`(5) / `环境情景`(1) / `口径依赖`(2) / `模型结构差异`(1) / `已检查因素累计`(2)，
  图例 5 项（文案 `累计量级`）。为容纳 11 行，面板高度比由 [1.18, 1] 调为 [1.0, 1.15]。
- **`fit_left()` 修好“白占宽”**：旧实现无条件在原 left 上**再加**一份标签宽度，
  fig5 因此把绘图区右推到 0.588（左侧约 2 in 空白）。改为“当前 left 够用就不动、不够才右移”，
  left 回到作者设定的 0.30，绘图区宽度 **2.48 → 4.04 in**。该函数只有 fig5 调用，
  因此其余 9 张图的字节未受影响（见下）。
- 尺寸 6.3×4.15 → **6.3×4.35 in**（纵横比 0.66 → 0.69，仍在 `ASPECT_EXEMPT[fig5]` 的 0.70 上限内）：
  行数由 7 → 7+11 后原高度会让 7.5 pt 刻度名互相压盖。

**fig10_力学升级（6.3×3.4 in，数值与硬编码标注）**
- 断言由旧标定值换成现行 `mechRefit.csv`/`mechRefit_fields.npz` 实测值：
  RMSE **0.502 → 0.522 mm**、三段 **1.135/0.843/0.161 → 1.193/0.875/0.163 mm**、
  平台模型值 **1.1751 → 1.1747 cm**、max|残差| **1.52 → 1.59 mm**、
  De 上界判别 1e-2 → 2e-3；新增 `t_f_h=51.1667 h` 与末端平衡表断言。
- **硬编码文字改数据现算**：(b) 面板“前 21 h：系统性欠缩（峰值 **+1.24** mm）”
  → 由 `res[k_max]` 现算（**+1.59 mm**），箭头 xy 由 (1.5, 1.235) 改为真实峰值点 (2.0 h, 1.5932)。
- **末端平衡表进诊断**：从 npz `terminal`（β=0.02…0.85 → J_end）线性内插拟合 β=0.1059 →
  **J_end=0.3343**、R_end≈1.156 cm，写进断言与 [F10] 行。
  说明：(c) 面板画的是**吸力上限** $1/[(1-\beta)(2-\beta)]p^*$=**0.591 p\***（β 取自 npz `fit_params`），
  与末端平衡表的 $J_{end}$（截面面积比 (R/R₀)²）**不是同一量**；
  (c) 面板已被曲线/De 框/上注排满，故不再加文字，末端平衡表由断言 + stdout 承载。
- 口径文档指针由作废的 `力学升级_重标定.md` 改为 `力学升级_修复重标定.md`；
  `[F10]` 行也改为读 npz 的 `t_f_h`（不再誊写结果）。图内文字无其它硬编码旧值。

**fig7_内生收缩贴合（未改）**：核对结论是不需要改。它画的是**单向标定贴合度**
（`endogenous_calibrated.csv`，附件 2 全 72 h 真实网格、非“停算后常值填充”），
RMSE **0.0473 mm**、平台 pred 1.1969 vs data 1.198、max|残差| 0.1618 mm 全部由数据现算，
与论文图 5 图注（0.047 mm / ±0.16 mm）一致；
论文里的 **0.088 mm** 是**另一个口径**（耦合主解满 72 h 全程，`q4_endo_eval.csv`：
0--达标 0.0646 / 达标--72 h 0.1272 / 全程 0.0879 mm），由正文 §6.6 承担，不进本图。

**顺手修好两处“重跑即崩”**（都是第三波改了 csv 表头/列而脚本没跟）：
- `fig4`：`结果总账.csv` 的“问题4主值(未舍入右端点,内生几何)”行含英文逗号与引号，
  旧代码按逗号裸切 + `startswith` 匹配失败 → `KeyError: 'q4'`；
  改用 `read_csv_rows()`（CSV 感知）按 `类别=t_f` + `项目` 前缀取 `值` 列，并断言 q3/q4 都在。
  `import re` 随之成为无用导入，已删。
- `fig5`：见上（`error_budget.csv` 的 `类型` 列）。

**复现与门禁**：`python plot_all.py` 一次出 10 张，**10/10 审计 PASS**、无缺字告警、退出码 0；
各图设计尺寸 5.4–6.3 in、最小字号 7.5 pt、Okabe-Ito 色板与语义映射未变。

**改动范围（md5 证据，`git show HEAD:` 对比）**：
- **内容改变：fig5、fig10**（PNG 与 PDF 都变）。
- **未变：fig1/2/3/4/6/7/8/9**——PNG **字节完全相同**；PDF 仅 `/CreationDate` 元数据不同
  （剥掉时间戳后逐字节相同，见本波核对脚本输出）。
