# A 题最终方案（定稿，2026-09-11）

> **本文档是唯一执行依据（single source of truth）。** 与本文冲突的旧文档一律以本文为准：
> - `07-笔记/建模原则-解析优先.md` 的"本阶段不产出求解代码"约束**作废**——交付数值由题给方程的数值解产生，解析用于判据、对拍与严格界。
> - `07-笔记/00-索引.md` 及 r1–r3 笔记、astra 方案中的**未复算数值**（57.45 h、[70,135] h、严格下界 15.716 h / 8.364 h 等）一律不得直接引用，只能作为待复算线索。
> - 评分依据 = `07-笔记/评委品味与路线选择.md`（E1–E16 口径与 T1–T14 任务），本文将其压缩为可执行条款。
> - 证明素材 = `other/A题/2026国赛A题_完整建模方案.md`（质量坐标、累计水量比较下界、守恒离散），本文采用其数学骨架，裁掉其非仿射 ε 族等装饰性内容。

---

## 1. 路线定案（F 路线）

**答案只来自题给方程的守恒有限体积数值解；解析结构只做三件事：定量判据、独立对拍、严格下界。**

四问 = 一套耦合方程 + 四个开关（物性附录 / 边界数据 / 几何是否演化 / 终点条件）：

| 开关 | 问题1 | 问题2 | 问题3 | 问题4 |
|---|---|---|---|---|
| 物性 | 附录2（常数，D=f(C)） | 附录3 | 附录3 | 附录4 |
| 耦合 | 热湿解耦 | 双向耦合 | 双向耦合 | 双向耦合 |
| 几何 | 固定 R=0.02 m | 固定 | 固定 | R(t) 外生（附件2） |
| 环境 | 附件1（0–1800 s） | 附件1+外推 | 同左 | 同左 |
| 输出 | 场（表1/2，result1） | 场（表3/4，result2） | 决策量 t_f（表5，result3） | 决策量 t_f（表6，result4） |

明确放弃：数据反演作主结果（撞 E1）、多物理扩展进主模型（撞 E1/E5）、纯降阶模型（无法交付空间分布）、网格收敛充当唯一验证（撞 E6）。

## 2. 模型定案

### 2.1 方程（质量坐标统一形式）

干物质标签 q∈[0,1]，U(q,t)=干基含水率，Θ(q,t)=温度(K)：

```
U_t = ∂_q(K U_q),                    K = D(U,Θ)/r_q²
a_eff(U)·Θ_t = (1/(r r_q))·∂_q(r k(U)/r_q · Θ_q)
```

边界（q=1）：`-D/r_q·U_q = h_m(U_s − C_env(t))`；`-k/r_q·Θ_q = h(Θ_s − T_a(t))`；q=0 正则对称。初值 U=2.55，Θ=301.15 K。

- 问题1–3：r(q,t)=R₀√q 固定域（K=4qD/R₀²）。
- 问题4：仿射收缩 r(q,t)=R(t)√q，长度不变，内部等比是**声明的最简闭合**（附件2只识别外半径）。

### 2.2 物性取值纪律（E1/E2，违反即整篇失分）

- 附录2/3/4 按问题分别使用，**不得调和、平均、常数化、用文献值替换**。
- D 中 C、T 取**局部瞬时值**，T 用开尔文（用摄氏度得 e^(−3850/50)≈0，程序不报错但全错）。
- ρ、c_p、k 逐点取值；a_eff=ρ·c_p 只作有效体积热容，ρ 不进湿分方程（附录3的 ρ(C) 在 C 小时与干物质守恒不相容，已证）。
- h=25、h_m=8e−7 从附录2延用到问题2–4，**声明为参数延用假设**。

### 2.3 必须声明的闭合假设（论文第一节主动写出）

1. **湿边界是有效闭合**：把附件1的空气水分浓度直接当作与 h_m 配套的驱动势 C_env。空气含湿量与药材干基含水率基准不同，这是本题最大单点风险（若改用解吸等温线 C_e≈0.137，分母 (0.15−C_e) 从 0.10 变 0.013，t_f 可增大 60% 量级）——**主结果用题目口径，等温线只进敏感性**。
2. **环境外推**：附件1终于 14400 s，其后主口径=末值保持（T_a=50.165 ℃，C_env=0.04986），分段线性插值保留采样值；对照口径见 §6。
3. **不含潜热**：热方程是有效显热近似，非完整能量闭合。依据：h 与 h_m 的等效能量需求相差 2.06–2.49 倍，潜热汇与附录物性不能同时自洽，故题目口径自洽的读法不含潜热。只在讨论节给出此量纲论证，不做修正算例。
4. **一维径向**：忽略端面与轴向（L₀/R₀=12.5 仅为几何信息，不保证误差界）。
5. **半径插值**：附件2 用 PCHIP 保形插值（核查半径为正、无反向），35 h 后末值 1.198 cm 平台延拓（声明为假设）。

### 2.4 达标判据

t_f = inf{t : max_q U(q,t) < 0.15}，用**未舍入值**判定；数值上报告穿越区间 + 右侧安全端点。最大值在全域重建场上取（不只用单元平均），并数值检验中心即最慢点（最大原理在单调边值下支持，但不作全情景公理）。0.15 干基 = 13.04% 湿基 ≈ 药典水分限度，作解释性论述。

## 3. 交付口径（冻结，错了整份表作废）

| 文件 | sheet | A列 | 列头 | 终点 |
|---|---|---|---|---|
| result1.xlsx | 温度、水分浓度 | 1–1800 s（1800 行，从 1 起） | 0,0.1,…,2.0 | 1800 s |
| result2.xlsx | 温度、水分浓度 | 1 s 步长，从 1 起 | 同上 | **物理口径：问题3同模型的 t_f + 余量**，不取整 72 h |
| result3.xlsx | 水分浓度 | 60 s 步长，从 60 起 | 同上 | t_f，追加结束行 |
| result4.xlsx | 水分浓度 | 60 s 步长，从 60 起 | 0,0.1,…,1.9,**药材表面**（21 列；依据：官方 PDF 表6 colspan=4） | t_f，追加结束行 |

- 全部四位小数；result4 超出当前表面的固定列**留空**，不外推不填零；表面列按模板单列。
- 论文表格从同一数据源抽取（结果总账表，保证摘要数字=正文数字）。
- 一条命令产出 result1–4，无人工干预，两次运行哈希一致。

## 4. 求解器（02-代码）

- 空间：固定 q 网格有限体积，N=200 起步；共享通量；中心 L=0；表面保留半单元阻力 + 外传质阻力串联（不得把末单元平均当表面值）。
- 时间：后向欧拉（保正 M-矩阵结构）或 TR-BDF2；自适应步长（一步 vs 两半步比较，超预算拒步）；时间节点含环境折点、输出时刻、阈值细分点。
- 非线性：系数冻结 Picard 迭代至状态增量与残差双达标（起点：ΔU<1e−9，ΔΘ<1e−7 K，再收紧十倍确认无感）；失败缩小步长，**不靠截断负值**。
- 输出重建：守恒重建 + 单元内极值检查（max 判据用）；中心值由正则性重建；q↔r 映射按当前几何。
- 先粗后精：第一天用 N=20 粗模型跑通问题2/3，确认 t_f 量级（预期 ~57 h）后再加精。

## 5. 验证体系（每层写明能排除什么）

| 层 | 手段 | 数学结构 | 能排除 / 不能排除 |
|---|---|---|---|
| V1 | 问题1热场解析解（圆柱 Robin 特征值 + 分段线性环境 Duhamel 卷积，保留 T_a(0)≠T₀ 项） | 谱解 vs 差分，**真独立**（E7） | 离散/边界/时间积分错误；不能排除物性取值错误 |
| V2 | 常 D 湿场参考问题特征展开对拍 | 同上 | 几何与湿边界离散错误 |
| V3 | **严格下界闸门**：t_f 主值必须大于经复算证明的比较下界，否则判错重算 | 不等式，与离散无关 | 排除错误的达标时间 |
| V4 | 守恒恒等式：总水量平衡 dŪ/dt = −b(U_s−C_env)，逐步相对残差 ≤1e−8 且随网格下降 | 恒等式 | 实现错误（一致性，非独立验证） |
| V5 | 退化测试：问题4固定 R + 附录4 应退回同物性固定域解；D=const 退回 Bessel 级数 | 开关测试 | 移动域实现错误 |
| V6 | 网格/时间 Richardson 收敛（阶数 ≥1.8） | 一致性 | 只证明离散收敛 |

**V3 待裁决（P1 任务）**：三个候选下界——astra 方案比较界 13.58 h（问题3）/13.15 h（问题4）、笔记 15.716 h / 8.364 h、评委文档膜界 11.2 h。只采用**重新推导并复算通过**的最强一个；复算前一律作顾问值，不作闸门。

## 6. 判据族与对照实验（论文第三节，E4 主战场）

判据族（全部由题给参数算出）：Le=α/D∈[10.8,268]（附3）/[31.4,233]（附4）；Bi_m=h_mR/D∈[1.19,20]/[6.4,42]；Bi_h=hR/k∈[1.04,3.42]。**每条判据必须配"用了它，t_f 偏多少"的反验列**（简化解 vs 全耦合解实测差值），没有反验数字的判据不进论文。

必做对照（敏感性矩阵，每组报 Δt_f）：
1. **问题4效应拆分**（全场最便宜最出彩）：附录4+固定R vs 附录4+R(t)，分离收缩效应与公式效应；t₃ 与 t₄ 的大小关系必须给机制解释（E8）。
2. 环境外推三档：末值保持（主）/ 线性外推 / 末1h均值。
3. 边界汇两档：题目口径（主）/ 山药解吸支 C_e=0.1366（只入敏感性，预期 t_f→82 h 量级）。
4. 半径插值两档：线性 vs PCHIP（预期差 ~0.6 h，与报告精度同量级）。
5. 收缩停止讨论：附件2 在 35 h 冻结而 t_f≈57 h，用 R(t)+声明的体积关系反推平均含水率做一致性核对（T7）；结皮/玻璃化只作讨论。
6. 潜热：只给 §2.3-3 的量纲论证一段。

## 7. 不确定度（E11）

t_f 报告 = 主值（未舍入值判定）+ 区间 + 误差预算表。预算 ≥6 源：空间网格、时间步、非线性迭代、输出重建/插值、半径插值、舍入（60 s 网格 + 四位小数本身产生 0.1–0.2 h）。确定性合成（三角不等式），不冒充统计区间。**C_e 口径产生的 60% 偏移不进区间**——区间一半以上宽度来自不可辨识题外参数 = 没解决问题；C_e 单列敏感性。

## 8. 执行清单

### P0（未完成则一切无效）
- T1 问题1求解器跑通 + result1.xlsx 落盘 + V1 对拍（逐点最大偏差随 N 收敛）。
- T2 交付口径冻结（§3 表格落成 `03-数据/交付口径.md`，依据模板原文；result2 终点用物理口径）。
- T3 环境外推定案 + 三档 Δt_f 数字。

### P1
- T4 问题3主结果：主值 + 收敛序列 + 舍入自洽 + 过 V3 闸门。（外部锚点：三份独立计算 56.92/56.93/57.45 h 在 1% 内一致，主值预期 ~57 h ≈ 2.4 天，与题干 2–3 天吻合。）
- T5 守恒与量纲门禁入库（每步残差 ≤1e−8 随网格下降；量纲表逐条过）。
- T6 问题4效应拆分对照三件套（§6-1）。
- T7 收缩停止一致性讨论。
- T8 下界复算裁决（§5 V3）。

### P2
- T9 判据族 + 反验列；T10 敏感性矩阵 ≥4 组；T11 误差预算表；T12 单命令复现 + 哈希一致。

### P3
- T13 写论文（结构见 §9）；旧验证脚本（08-临时，三处有 bug）**不得未经修复复用**。

## 9. 论文结构（八节）

1. 问题重述与审题：四问开关表 + **主动写出三个陷阱**（三套物性按问使用；D 中 T 用 K、C 取局部；附件1只有4 h / 附件2在35 h 冻结）。
2. 模型建立：统一质量坐标方程 + **参数取值表**（符号/数值/单位/来源/取值位置，题目参数与声明假设分栏）+ 闭合假设声明（§2.3）。
3. 定量判据：判据族 + 达标判据 + 严格下界，每条配"偏多少小时"。
4. 算法与实现：离散、迭代、收敛准则、一条命令复现。
5. 独立验证：V1–V6 分层，写明每层能/不能排除什么。
6. 结果与解释：四问递进；t₃ vs t₄ 机制归因；0.15 的药典含义。
7. 误差与不确定度：预算表，区分数值误差/参数不确定性/闭合偏差三类。
8. 结论与局限：哪些结论是题目口径的，哪些依赖声明假设；内部收缩不可识别列为局限（一句话，不做 ε 族）。

## 10. 风险排序（防守动作）

1. **边界汇口径**（唯一能量级翻盘：57→90 h 量级）→ 题目口径为主 + 敏感性单列 + 假设声明前置。
2. **交付断链**（当前 02-代码/04-结果/06-论文全空）→ P0 先行，粗模型第一天出量级。
3. **环境外推** → 三档敏感性。
4. **移动域表述与半径插值**（合计 1–5%）→ V5 退化测试 + 插值两档。
5. **附件1波动**（±0.5 ℃，95 处回落）→ 原样输入为主口径，滤波作敏感性；影响第四位小数与 0.1–0.2 h。
